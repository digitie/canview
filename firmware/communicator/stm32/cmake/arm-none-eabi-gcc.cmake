set(CMAKE_SYSTEM_NAME Generic)
set(CMAKE_SYSTEM_PROCESSOR arm)
set(CMAKE_TRY_COMPILE_TARGET_TYPE STATIC_LIBRARY)

if(DEFINED ENV{ARM_GNU_TOOLCHAIN_ROOT})
    list(PREPEND CMAKE_PROGRAM_PATH "$ENV{ARM_GNU_TOOLCHAIN_ROOT}/bin")
endif()

if(DEFINED ENV{CANVIEW_ARM_GCC_EXECUTABLE})
    set(CMAKE_C_COMPILER "$ENV{CANVIEW_ARM_GCC_EXECUTABLE}" CACHE FILEPATH "Arm GNU C compiler")
    set(CMAKE_ASM_COMPILER "$ENV{CANVIEW_ARM_GCC_EXECUTABLE}" CACHE FILEPATH "Arm GNU assembler compiler")
else()
    find_program(CMAKE_C_COMPILER arm-none-eabi-gcc REQUIRED)
    find_program(CMAKE_ASM_COMPILER arm-none-eabi-gcc REQUIRED)
endif()

if(DEFINED ENV{CANVIEW_ARM_OBJCOPY_EXECUTABLE})
    set(CMAKE_OBJCOPY "$ENV{CANVIEW_ARM_OBJCOPY_EXECUTABLE}" CACHE FILEPATH "Arm GNU objcopy")
else()
    find_program(CMAKE_OBJCOPY arm-none-eabi-objcopy REQUIRED)
endif()

if(DEFINED ENV{CANVIEW_ARM_SIZE_EXECUTABLE})
    set(CMAKE_SIZE "$ENV{CANVIEW_ARM_SIZE_EXECUTABLE}" CACHE FILEPATH "Arm GNU size")
else()
    find_program(CMAKE_SIZE arm-none-eabi-size REQUIRED)
endif()

set(CMAKE_EXECUTABLE_SUFFIX_C ".elf")
set(CMAKE_EXECUTABLE_SUFFIX_ASM ".elf")
