cmake_minimum_required(VERSION 3.25...4.4)
if(NOT DEFINED ENV{IDF_PATH})
    message(FATAL_ERROR "Set pinned IDF_PATH via tools/environment/setup-windows.ps1")
endif()
set(EXTRA_COMPONENT_DIRS "${CMAKE_CURRENT_LIST_DIR}/../components")
# Legacy draft components are deliberately excluded from the foundation image.
set(COMPONENTS main)
include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(canview_diagnostic_bridge)
idf_build_get_property(core_sdkconfig SDKCONFIG)
idf_build_get_property(core_python PYTHON)
set(core_config_gate "${CMAKE_CURRENT_LIST_DIR}/../../tools/check_sdkconfig.py")
set_property(DIRECTORY APPEND PROPERTY CMAKE_CONFIGURE_DEPENDS "${core_config_gate}")
execute_process(COMMAND "${core_python}" -B "${core_config_gate}" "${core_sdkconfig}"
    --board bridge-r1-n8r2 RESULT_VARIABLE core_config_result)
if(NOT core_config_result EQUAL 0)
    message(FATAL_ERROR "Bridge bench sdkconfig contract failed")
endif()
