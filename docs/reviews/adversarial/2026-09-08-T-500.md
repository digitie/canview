# T-500 protocol/CAN fault bench와 HIL harness 적대적 리뷰 closure

- 리뷰 기준선: `50410ba23fcecfa1f28cea837d04a061c201d648` → `ff3121ce04328ff61a73f13492f8be9927f0dc98`
- 대상: `tests/hil/` deterministic host harness, read-only rig adapter 계약, JSONL event, machine-readable evidence와 validator
- 리뷰 범위: embedded runtime/안전 경계와 protocol·security·build·evidence integrity 관점의 독립 검토
- 범위 밖: 실제 MCU 실행, board flash, power/reset fault injection, physical CAN analyzer, 차량 bus, provisioning, 다른 OS/Python 조합의 완전한 검증
- 물리/HIL 상태: `NOT_RUN`
- 차량 CAN TX: `NO-GO`

## 기준선과 격리

두 reviewer에게 동일한 candidate/base와 scope manifest를 전달하고 서로의 finding을 공유하지 않았다. 각 reviewer는 `F:/dev/canview-wt/t500-hil`에서 candidate commit을 immutable Git object로 확인했으며, 시작과 종료 시점의 worktree가 clean이고 `HEAD`가 candidate와 일치함을 기록했다. source와 test 파일은 candidate checkout 및 `git show`로 읽었고, 임시 fixture와 출력은 저장소 밖에서 생성했다.

최종 candidate는 base의 descendant이며 다음 commit에 포함된 T500 수정 전체를 검토했다.

| Commit | 목적 |
|---|---|
| `7c63183` | deterministic fault harness 최초 구현 |
| `8a1f13d` | 초기 A/B review의 evidence·입력 경계 finding 수정 |
| `3e49ea8` | 후속 malformed input·failure identity finding 수정 |
| `01632f7` | selection·custom structural-only·scalar schema finding 수정 |
| `9790933` | deep input·report size·FAIL completeness finding 수정 |
| `ff3121c` | Unicode JSONL 값 보존을 위한 ASCII LF record boundary 수정 |

## 독립 최종 reviewer 결과

| Reviewer | 실행 ID | 전문 범위 | 실행 시각(UTC) | 결과 |
|---|---|---|---|---|
| A | `CV-HOSTILE-20260908-T500-A-10` | typed event, JSON 경계, runtime failure identity, JSONL round trip | 13:46:32–13:49:27 | `PASS` |
| B | `CV-HOSTILE-20260908-T500-B-10` | selection/completeness, replay identity, structural-only, schema·Unicode framing | 13:46:35–13:51:13 | `PASS` |

- [Reviewer A raw report](evidence/2026-09-08-T-500-reviewer-a.md)
- [Reviewer B raw report](evidence/2026-09-08-T-500-reviewer-b.md)

두 reviewer 모두 candidate `ff3121ce04328ff61a73f13492f8be9927f0dc98`와 base `50410ba23fcecfa1f28cea837d04a061c201d648`를 확인하고 실제 source/test를 읽고 재현 시험을 실행했다. 최종 report의 finding 수는 다음과 같다.

| 등급 | Reviewer A | Reviewer B | 통합 결과 |
|---|---:|---:|---:|
| P0 | 0 | 0 | 0 |
| P1 | 0 | 0 | 0 |
| P2 | 0 | 0 | 0 |
| P3 | 0 | 0 | 0 |

## 이전 finding disposition

초기 A-02/B-02는 candidate `7c63183`에서 P1/P2 finding을 보고하여 `BLOCK`했다. 이후 source와 regression test를 수정하고 각 단계의 raw report를 보존했다. A-03/B-03 및 A-04/B-04에서 재확인된 잔여 항목도 다음 단계에서 수정했다.

| 이전 review 단계 | 핵심 finding | disposition 및 근거 |
|---|---|---|
| A-02/B-02 | malformed event·typed value, stale PASS, output redirection, bounds·resource 처리 | `8a1f13d`, `3e49ea8`와 관련 regression test로 `FIXED`; 최종 A-10/B-10 재검토 |
| A-03/B-03, A-04/B-04 | 입력 경계, failure identity, report integrity의 후속 P2 | `3e49ea8`, `01632f7`에서 `FIXED`; 최종 source/test 재검토 |
| A-07 | deep JSON recursion 시 이전 PASS 보존, report writer/validator 크기 불일치 | `9790933`에서 controlled `BLOCKED`와 공통 8 MiB limit으로 `FIXED` |
| B-07 | FAIL explicit selection 미제한, custom assertion 분류 불일치, scalar type 혼동 | `9790933`에서 expected result identity, `structural-only`, strict scalar validation으로 `FIXED` |
| B-09 | U+0085/U+2028/U+2029를 JSONL record delimiter로 오인할 수 있는 framing | `ff3121c`에서 ASCII LF-only split 및 byte-offset validator 일치로 `FIXED`; B-10 재검토 |

잘못된 candidate hash가 전달되어 유효하지 않았던 A-05/B-05와 A-08/B-08 raw 기록, reviewer service가 최종 결과를 반환하지 못한 A-06/B-06 기록도 그대로 보존했다. 이 incomplete 또는 invalid 실행을 PASS로 산정하지 않았으며, 최종 판정은 정확한 candidate에서 완료된 A-10/B-10만 사용했다.

## 최종 재검증

| 검증 | 결과 |
|---|---|
| `python -B -m unittest tests.test_hil_runner -v` | 46/46 PASS |
| `python -B -m unittest discover -s tests -p "test_*.py"` | 97/97 PASS |
| host CLI inventory | 12/12 PASS |
| host evidence validator | PASS |
| `brownout` explicit selection + `--expected-scenario brownout` | PASS |
| g2 read-only rig adapter | 장비 미연결 `SKIPPED`, validator PASS |
| `python -B -m compileall -q tests/hil tests/test_hil_runner.py` | PASS |
| document link validation | PASS |
| plan validation | PASS |

최종 시험은 deep scenario/report, 8 MiB 경계, malformed UTF-8·lone surrogate, duplicate/replay, result substitution, FAIL completeness, custom structural-only assertion, typed scalar/container mutation, Unicode JSONL 값과 first-failure byte offset을 포함했다. 이전 PASS를 보존한 채 입력 오류를 숨기는 경로는 모두 controlled `BLOCKED` 또는 validator rejection으로 확인했다.

## 남은 gate와 결론

host harness의 준비와 방어적 evidence 계약은 완료되었다. 그러나 실제 board flash, MCU task 실행, physical power/reset/brownout, CAN analyzer, 차량 CAN 연결, provisioning과 장시간 resource/concurrency 시험은 장비가 없어 `NOT_RUN`이다. 따라서 이 report는 harness 범위의 `PASS`이며 G2/G4 physical 또는 차량 기능 승인으로 해석하지 않는다. Diagnostic/read-only 경계상 차량 CAN TX는 계속 `NO-GO`다.

**통합 verdict: `PASS` (T-500 host-harness 범위). 미실행 physical/HIL gate는 `NOT_RUN`, 차량 CAN TX는 `NO-GO`.**
