# T-400 P2 board/runtime handoff 적대적 리뷰

## 기준선과 범위

- base: `c8a725d0071e84a1329c30e2cca4409b4a42cfba`
- 최초 candidate: `f35779a78603dd8241ead9a8baa7307d629bf33a`
- 격리: reviewer별 `origin/codex/t400-bridge-handoff` fetch 뒤 immutable object-only `git show`/`git diff`; checkout·source 수정·stage·commit·push 없음
- 범위: board identity/profile, wrong-BSP 거부, ESP runtime callback/ISR/pool contract, Communicator ESP/Bridge safe GPIO 실패 정책
- 범위 밖: SoftAP, HTTP/WebSocket, 인증, CAN transport/control, raw replay, control lease, vehicle TX

Bridge의 read-only 관계와 CAN TX NO-GO는 이 review에서 변경되지 않는다. flash/HIL, rail/reset/brownout, provisioning, vehicle CAN/evidence와 vehicle TX release는 모두 `NOT_RUN`이다.

## reviewer 실행과 원문

| reviewer | execution ID | 결과 | 원문 |
|---|---|---|---|
| A attempt 1 | `RVA-20260907T211800-KST-01` | candidate short SHA가 reviewer object database에 없어 line-level 검토 불가 `BLOCK`; PASS로 변환하지 않음 | [원문](evidence/2026-09-07-T-400-reviewer-a-attempt-1-incomplete.md) |
| B attempt 1 | `cc3ae311-bfb6-438f-bfa8-acdc2b2a2584` | candidate short SHA가 reviewer object database에 없어 line-level 검토 불가 `BLOCK`; PASS로 변환하지 않음 | [원문](evidence/2026-09-07-T-400-reviewer-b-attempt-1-incomplete.md) |
| A | `A-OBJ-f35779a-20260907T211614+0900` | P1 1건, P2 2건, `BLOCK` | [원문](evidence/2026-09-07-T-400-reviewer-a.md) |
| B | `RB-20260907T212127+0900-f35779a` | P1 1건, P2 2건, `BLOCK` | [원문](evidence/2026-09-07-T-400-reviewer-b.md) |

두 완전 report는 full 40-hex candidate를 fetch한 뒤 실제 candidate source/config/test를 읽었다. reviewer 간 finding은 최종 report 전에 공유하지 않았다.

## finding과 disposition

| ID | 등급 | 실패 형태 | disposition |
|---|---:|---|---|
| T400-P1-01 | P1 | `app_main()`이 wrong BSP의 `enter_safe_state()`를 profile 비교 전에 실행해 부적절한 GPIO를 먼저 만질 수 있다. | `FIXED—재검토 대기`: app이 한 번 복사한 BSP port를 `canview_esp_board_preflight()`로 GPIO·idle·SDK open 전에 검증한다. 두 app+BSP 교차-link test는 GPIO/runtime open 0회와 terminal idle을 확인한다. |
| T400-P2-01 | P2 | board ID만 hash하면 같은 ID의 stale pin/manifest contract를 구분하지 못한다. | `FIXED—재검토 대기`: generator v2 digest는 canonical board manifest 항목과 source bytes를 함께 입력으로 사용한다. manifest/source mutation과 profile collision negative test를 추가했다. |
| T400-P2-02 | P2 | `canview_esp_runtime_open()`이 ISR에서 task API와 runtime state를 변경할 수 있다. | `FIXED—재검토 대기`: 모든 task API·state 변경 전에 ISR guard를 두고 direct open/BSP wrapper fixture가 runtime·port 보존을 확인한다. |
| T400-P2-03 | P2 | pool init이 context validator를 호출하지 않아 ISR에서 pool state를 초기화할 수 있다. | `FIXED—재검토 대기`: pool init도 validator를 상태 변경 전에 호출하며 ISR fixture가 uninitialized state 보존을 확인한다. |

`FIXED—재검토 대기`는 coordinator source 수정과 로컬 회귀 검증만 뜻한다. P0/P1 closure 또는 merge 권한을 뜻하지 않는다. original reviewer A/B가 post-fix immutable candidate를 다시 읽어야 한다.

## post-fix 재검증

- `python -B tools/generate_boards.py --check` 통과
- pinned Windows Host Debug/Release CTest: 장시간 `uart-fault-stream` 제외 각각 `110/110` 통과
- ESP core/STM32/공용 coverage gate 통과
- ESP-IDF 6.0.3 및 Arm GNU 15.3에서 STM32 Debug/Release, Communicator·Bridge·Controller·public fixture의 BIN/ELF/MAP 18개 생성

위 target 재생성은 post-fix commit 전 dirty metadata에서 수행했다. post-fix immutable commit의 target artifact/warning과 PR CI는 별도 gate이며 아직 `NOT_RUN`이다.

## 남은 gate와 판정

post-fix immutable candidate의 reviewer A/B 재검토, CI target artifact/warning scan, local WSL ASan/UBSan 재시도 및 physical/HIL gate가 남아 있다. local WSL ASan/UBSan은 `E_ACCESSDENIED`로 `NOT_RUN`이며 CI sanitizer는 별도 확인한다. board flash, ST-LINK/USB serial, power/reset/brownout, PSRAM/clock/watchdog 장시간, vehicle CAN/evidence, provisioning과 vehicle TX release도 `NOT_RUN`이다.

**현재 verdict: BLOCK.** 최초 P1은 수정됐지만 original reviewer의 post-fix line-level verdict와 CI가 없으므로 PR #25를 ready 또는 merge하지 않는다.
