# T-107 BSP identity·공개키 중간 적대적 리뷰

- Review ID: `2026-09-19-T-107-trust`
- 종류: 전문 서브에이전트 2인의 독립 object-only source 리뷰
- Candidate: `6c14950d89bfe01f216dea33b58933dfc9e445e4`
- Base: `06383d6cf9aa8b0c68646f96b3507e2144ca074e`
- 대상: [T-107](../../tasks/T-107-stm32-mcuboot.md)의 BSP const identity, DER 생성기,
  CMake 입력/의존성, 관련 C/Python 시험과 문서·합성 fixture delta14개
- 범위 밖: 최종 bootloader ELF/실기 부팅·SRAM/WRP·제조 승인·T-205 floor·전체 task/merge
- Coordinator: Codex
- 상태: `CONDITIONAL`, B P2 수정 후 원 reviewer 확인 대기

## 공통 manifest와 격리

동일 candidate/base/scope manifest를 두 reviewer에게 전달했고 개별 전문 범위만
다르게 배정했다. 전달 원문·명령·읽은 파일·시각은 아래 evidence에 보존했다.
두 원본을 저장한 뒤 교차 확인했다. 두 reviewer 모두 commit object만 읽었으며
worktree clean 확인이나 target/host 시험을 직접 수행했다고 주장하지 않는다.

| 항목 | A | B |
|---|---|---|
| 전문 범위 | C/runtime·const·메모리·BSP/SDK ABI | 보안·DER·CMake·시험 oracle·CI |
| Agent ID | `01a0b94f-d65f-7ab0-90ae-b43a0d4971a8` | `01a0b94f-d774-7352-9e98-6d4bec9c9ada` |
| UTC 시작/종료 | 10:57:12 / 11:00:03 | 10:57:13 / 11:00:40 |
| 관찰 candidate/base | 위 전체 hash 일치 | 위 전체 hash 일치 |
| 격리/clean | object-only / N/A | object-only / N/A |
| 원본 | [A](evidence/2026-09-19-T-107-trust-reviewer-a.md) | [B](evidence/2026-09-19-T-107-trust-reviewer-b.md) |
| 최초 verdict | PASS | CONDITIONAL |

## Finding과 교차 확인

A는 P0/P1/P2/P3 0건, B는 P2 1건이다. 상대 원본을 보고 최초 판정을 변경하지 않았다.

| ID | 등급 | 위치 | 실패·영향 | 권고·disposition |
|---|---|---|---|---|
| B-BOOT-TRUST-01 | P2 | `bootloader/trust.cmake:2,13`, `tests/ota/test_stm32_boot_trust.py:105,110` (candidate) | 최초 untyped CLI 상대 public DER가 FILEPATH cache 선언에서 절대경로로 바뀌어 계약을 통과. 기존 시험은 typed cache와 존재하지 않는 상대 파일이라 결함을 놓침 | 원문 STRING cache와 실제 유효 상대 파일의 fresh configure 시험. 수정/회귀는 아래와 같으며 원 reviewer 확인 전 OPEN |

기본 키 도입이나 서명 우회가 확인된 것은 아니다. 그래도 빌드 디렉터리에 따라
다른 파일이 선택되는 입력 계약 결함이므로 defer하지 않고 수정한다.

## 재현·수정·검증

1. 유효 DER를 둔 fresh cache 회귀를 먼저 추가했다. Windows CMake4.4.3와 WSL GNU
   모두 상대 파일을 받아 configure0이 되어 기대 거절 assertion이 실패했다.
   `build/t107-trust-relative-{windows,regression}-before.log`.
   최초 직접 PowerShell 시도는 인수 분리로 실패했으며 재현 근거로 계산하지 않는다.
2. cache를 FILEPATH에서 STRING으로 바꿔 원문 상대경로를 보존한다. typed FILEPATH
   명시/미명시의 fresh2개와 기존 typed cache 상대 입력을 모두 검증한다.
3. Windows 전체 trust 시험1/1 18.75초, GNU Python4개+통합 시험 통과.
   `build/t107-trust-relative-{windows,gcc}-after.log`. 제품 C/암호 코드는 바꾸지 않았다.
4. source hash `e733f54506616f3379de6fc3a485e80f718419294334dc6237c4c7dc758260bd`로
   합성 fixture를 갱신하고 관련13개 시험을 통과했다. physical capture로 승격하지 않는다.

원 A/B가 post-fix commit에서 각 finding과 전체 delta를 다시 확인해야 한다.
원본 공백도 보존하며 리뷰 원문의 trailing blank 공백은 코드 경고와 구분한다.

## 최종 경계

이 기록 시점 B P2는 OPEN이며 최초 CONDITIONAL을 PASS로 바꾸지 않는다.
Physical/HIL·Flash/provisioning NOT_RUN, 차량 TX NO-GO, Bridge read-only다.
전체 task/PR37 merge 승인은 아니다. 후속 검토는 새 report에 보존한다.
