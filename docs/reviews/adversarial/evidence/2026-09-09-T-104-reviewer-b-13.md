# T-104 B-13 원문 보존

Reviewer가 작성한 보고서 원문이다. 임시 경로는 원문 문맥이며 저장소 navigation이 아니다.

````text
# T104-RB-20260909-13 — 원 reviewer closure 보고

- 시작: 2026-09-09T18:27:20.4274066+09:00
- 종료 소스 검사: 2026-09-09T18:28:45.7668093+09:00
- 전문 범위: STM32 UART CTS, DMA TX buffer lifetime와 reset 소유권, time-sync clock domain/측정 결합/만료, UART coverage.
- Candidate: `6b33a59bec4ba0285cedc3d19c983e5f2dd9c162`
- Initial reviewed: `9066379e58db9c423e6288c0e5b994b5fe671a45`
- Task base: `b17bdfc0bb2a1bfa9d300c1e7662cac05c96df40`
- 소스 worktree: `F:/dev/canview-wt/2026-09-09-t104-b13`

## 격리

시작과 종료에 HEAD가 지정 candidate와 일치했다. `symbolic-ref -q HEAD`는 출력이 없고 detached 상태였다. `status --porcelain`은 출력이 없어 clean이었다. 전역 ignore 파일의 읽기 권한 경고가 있었지만 Git 명령은 실행됐다. safe.directory는 해당 PowerShell 프로세스 환경변수에만 지정했으며 전역 설정을 변경하지 않았다.

이번 B-13 실행에서는 다른 reviewer 결과, issue, docs/reviews, resume, journal을 읽지 않았다. 기존 대화 문맥은 유지되지만 이번 판단은 지정 candidate의 소스와 새 실행 결과로 했다. AGENTS의 resume 참조 지침보다 이번 명시적 읽기 금지를 우선했다. 소스 수정·commit·push는 하지 않았다. 새 build 및 coverage 산출물은 본 작업 `work/review13`에만 기록했다. 설치·환경 우회·실패 재시도는 없었다.

## 실제 읽은 파일과 범위

아래 경로는 위 candidate worktree 기준이다. 긴 전체 diff를 읽었다고 주장하지 않으며, 이번 요청대로 원 finding 관련 bounded range를 읽었다.

- `AGENTS.md`: 전체.
- `firmware/communicator/stm32/platform/stm32g474/uart_dma.c`: 250–360, 465–527, 530–930행. CTS sampling, abort/reset hook, TX start, start/stop/service, RX/IRQ 연결을 확인했다.
- `firmware/communicator/stm32/platform/stm32g474/uart_dma.h`: 전체.
- `firmware/communicator/stm32/module/uart_link.c`: 식별자 검색 및 604–696, 994–1132, 1718–1805, 2100–2130행. clear/recovery, time-sync, direct reset과 tick을 확인했다.
- `firmware/communicator/stm32/interface/canview_stm_uart.h`: 관련 식별자 검색 및 1–38, 170–275, 319–340행. timeout 상수·context·reset hook·borrowed TX buffer 계약을 확인했다.
- `firmware/communicator/stm32/app/main.c`: 전체. UART platform start와 필수 scheduler worker 연결을 확인했다.
- `tests/stm32/test_uart_dma_platform.c`: 전체. CTS 상태, DMA 중단, old TX event 제거 및 새 TX 유지 회귀를 확인했다.
- `tests/stm32/test_uart_link.c`: 관련 시험 검색 및 100–124, 467–607, 1228–1325행. reset hook 호출/실패, 독립 epoch, pending 만료 및 COMMIT 경계를 확인했다.
- `tools/check_stm32_coverage.py`: 전체. 두 UART source의 계측 범위와 threshold 판정을 확인했다.

## 원본 finding disposition

| ID | 원래 실패·영향 | 판단과 현재 근거 |
|---|---|---|
| B-01 / P1 | CTS readiness 반전으로 흐름제어 상태를 잘못 해석 | **FIXED.** `uart_dma.c:252`는 ESP RTS high를 blocked로 반환하고 `:753`에서 runtime에 전달한다. platform 시험은 high에서 blocked, low에서 unblocked를 검증하며 통과했다. 이 시험을 실제 전기 흐름제어 측정으로 해석하지 않는다. |
| B-02 / P1 | reset/recovery가 DMA에서 사용하는 TX buffer를 지워 전송 데이터가 훼손될 수 있음 | **FIXED.** `uart_link.c:668` recovery와 `:1729` direct reset은 reset hook 성공 후에만 buffer를 지운다. 실패하면 즉시 반환한다. `uart_dma.c:323`은 DMA EN과 USART DMAT를 끄고 barrier/readback으로 중단을 확인한다. `:344`부터 critical section에서 이전 TX done/error와 NVIC pending을 정리한다. `:567`에서 platform hook을 설치한다. platform reset 회귀가 in-flight DMA 종료·old TC/TE 제거·RX event 보존·후속 TX의 phantom completion 부재를 검증했고 통과했다. owner 시험은 peer HELLO recovery hook 호출과 실패 전파를 검증했다. |
| B-03 / P2 | 독립 clock epoch의 정상 time-sync를 거부 | **FIXED.** `uart_link.c:1088`은 같은 domain의 t3≥t2, t4≥t1을 확인하며 `:1098`의 signed difference로 cross-clock offset을 계산한다. `test_uart_link.c:467`의 Controller 1000000 / STM 990100 사례가 정상 수락되고 음수 offset이 생성되는 회귀가 통과했다. |
| B-04 / P2 | COMMIT의 t2/t3를 STM의 저장 측정값과 결합하지 않아 조작된 mapping을 수락 가능 | **FIXED.** `uart_link.c:1083`–1088의 동일 조건문이 token/boot/generation/t1과 저장 t2/t3를 모두 비교한다. 불일치 시 mapping 설정 전에 STALE을 반환한다. 관련 time-sync 수락/거부 suite가 통과했다. 동일 token의 t2 또는 t3만 변조하는 독립 probe는 이번 실행에 추가하지 않았으므로 해당 두 비교 자체는 정적 검토 근거다. |
| B-05 / P2 | 중단된 pending 교환이 이후 동기화를 무기한 막음 | **FIXED.** interface 34행 timeout은 1000 ms이며 `uart_link.c:620`에서 그 이상이면 pending을 지운다. RX handler `:1000`은 새 요청의 busy 검사 전에 만료를 처리하고 tick `:2123`도 같은 helper를 호출한다. tick 만료 및 tick 없는 COMMIT의 999/1000/1001 ms 경계 시험이 통과했다. 만료 후 새 REQUEST 수락은 이번 실행에서는 제어 흐름으로 확인했다. |
| B-06 / P2 | 두 신규 UART 구현이 coverage gate에서 제외 | **FIXED.** coverage 도구가 uart-link와 uart-platform source 집합을 각각 정확히 비교하며 function/line/branch 기준 100/70/50을 판정한다. 두 그룹을 포함한 7개 그룹이 실제 새 build에서 모두 통과했다. 기존 core 기준 100/95/90은 유지된다. |

위 여섯 원 finding에 추가 수정 권고는 없다. 검토 범위에서 미해결 P0/P1 및 새 P0/P1/P2/P3는 확인하지 못했다. 따라서 새 finding의 file:line·failure impact·수정 권고 항목은 없다.

## 실제 실행 명령과 결과

VS native Developer PowerShell, 제공된 LLVM 23.1.0/CMake 4.4.3/Ninja 1.13.2/Python 3.14 경로를 사용했다. 아래 `<candidate>`는 위 worktree다.

```text
git -C <candidate> rev-parse HEAD
git -C <candidate> symbolic-ref -q HEAD
git -C <candidate> status --porcelain
cmake -S <candidate> -B work/review13/build -G Ninja -DCMAKE_C_COMPILER=clang -DCMAKE_BUILD_TYPE=Debug -DCANVIEW_COVERAGE=ON
cmake --build work/review13/build
ctest --test-dir work/review13/build -R 'stm32-uart' --output-on-failure
python -X utf8 -B <candidate>/tools/check_stm32_coverage.py --build work/review13/build
```

Configure/build 성공. 초기 P1 관련 회귀를 포함하는 `stm32-uart-link`와 `stm32-uart-platform`은 **2/2 통과**했다. Coverage gate는 **7개 그룹 모두 통과**했다.

| UART 그룹 | 함수 | 행 | 분기 |
|---|---:|---:|---:|
| owner | 73/73, 100% | 1394/1897, 73.484449% | 513/878, 58.428246% |
| platform | 36/36, 100% | 608/764, 79.581152% | 188/322, 58.385093% |

원문 로그: `work/review13/configure.log`, `build.log`, `ctest.log`, `coverage.log`. 계측 JSON은 `work/review13/build/stm32-coverage-mnee27kb`에 있다.

## 미검토·미실행 및 판단 범위

이번 실행은 원 reviewer의 여섯 finding closure다. Task base부터 전체 delta, 지정 파일의 나머지 모든 코드, 문서 정합성, target rebuild/artifact/CI, 외부 SDK 구현 전체를 새로 검토하지 않았다. 전체 host CTest를 실행한 것도 아니다. 관련 owner/platform 시험과 coverage 실행 범위만 주장한다.

실제 MCU DMA/IRQ timing, UART 4 Mbps 전기·PRBS·RTS/CTS, watchdog 및 물리 HIL은 **NOT_RUN**이다. 이 software closure를 하드웨어 승인이나 T-104 완료 선언으로 확장하지 않는다. 다른 reviewer의 검토를 대체하지 않는다.

**최종 verdict: PASS — 원본 B-01~B-06 closure 범위.**


````
