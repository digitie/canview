# 독립 재검토 원문 보존

- 에이전트 ID: `01a07797-9342-78b2-b5a1-b45be36936ea`
- 실행 입력: `313f180`을 기준으로 `027b22e`의 시험 delta를 Git object-only 방식으로 검토. 기존 자기 finding, 바이트 계수 soak, target source 동일성과 미실행 gate를 확인. 다른 reviewer 보고서는 열람하지 않음.
- 아래 제공자 원문은 수정하지 않고 보존한다.

# T-004 Post-fix Immutable Review Raw Report

## Execution

- Execution ID: `T004-IMMUTABLE-DELTA-20260907-01`
- Candidate: `027b22e6e22997e5405cbbdaeb95545037c55625`
- Previous reviewed candidate: `313f180cf4d9990cd10c31a89db7e110116e4c22`
- Scope: object-only `git show`/`git diff`
- Start: `2026-09-06T21:32:24.4301615Z`
- End: `2026-09-06T21:32:42.3202575Z`
- Other reviewer report: not read
- Compilation: not performed
- Moving worktree content: not used

## Delta verification

```text
git diff 313f180 027b22e
```

Changed files only:

- `CMakeLists.txt`
- `tests/protocol/test_uart.c`
- `tests/protocol/uart_fault_stream.py`

No changes under:

- `shared/protocol/`
- `protocol/schema/`
- `protocol/canview_uart_protocol.h`
- `firmware/`
- target build inputs

`git diff --check`: PASS.

## New soak review

The new C soak-smoke:

- Uses two distinct codec contexts.
- Computes required bytes as:

  `seconds × 4,000,000 bits/s ÷ 10 bits/byte`

- For 24 hours: `34,560,000,000` bytes per direction.
- Counts bytes actually passed to each production C decoder.
- Exercises changed, inserted, deleted, and oversize faults.
- Checks per-direction guards, counters, stream reset state, and fault accounting.
- Adds `uart-soak-smoke` CTest coverage.
- Adds `soak-24h` dispatch path without changing production code.

Coordinator-reported soak-smoke: PASS.

Full 24-hour run: **PENDING**. I did not wait for completion.

The previous Python virtual-time simulator remains insufficient as a 4 Mbps byte-budget proof. The new C soak addresses that specific evidence gap if the full run completes successfully.

## Findings

### P0

None.

### P1 — OPEN — borrowed payload ownership remains unresolved

Locations unchanged:

- `shared/protocol/include/canview_uart.h:96-98`
- `shared/protocol/include/canview_uart.h:287-288`
- `shared/protocol/include/canview_uart.h:317-327`
- `shared/protocol/src/canview_uart.c:1586-1604`

The delta modifies tests only. `canview_uart_command_dispatch_admit()` still passes a codec-backed borrowed `canview_uart_message_view_t` directly to the enqueue callback.

If the callback stores the view or payload pointer instead of deep-copying synchronously, the next codec feed can mutate the queued command.

Required action remains:

- enforce copy/ownership transfer in the queue API, or
- explicitly require deep-copy before callback return and add a payload-lifetime regression test.

Status: **OPEN / merge blocker**  
Disposition: **not fixed by 027b22e**

### P2 — OPEN — session reset atomicity remains unresolved

Locations unchanged:

- `shared/protocol/include/canview_uart.h:329-338`
- `shared/protocol/src/canview_uart.c:1608-1626`
- `shared/protocol/src/canview_uart.c:1649-1738`

The delta does not add synchronization or single-owner enforcement. Runtime concurrent reset/admission remains a follow-up gate.

Status: **OPEN / runtime follow-up**

## Gate disposition

- Production source changed by delta: **NO**
- Target inputs changed by delta: **NO**
- New byte-counted C soak-smoke: **reported PASS**
- Full 24-hour, 34.56 GB/direction soak: **PENDING**
- T-104/T-105/T-106/T-202/T-203/T-500 physical/runtime gates: **OUT OF SCOPE / NOT RUN**
- Original P1 ownership finding: **OPEN**

## Verdict

**BLOCK**

The new soak implementation correctly closes the earlier byte-budget evidence gap once the full run passes, but it does not address the original P1 ownership defect. Therefore the full soak result cannot make T-004 merge-ready by itself.
