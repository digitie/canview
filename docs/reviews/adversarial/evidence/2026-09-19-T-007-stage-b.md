# T-007 C stage 수신 순서 독립 리뷰 — Reviewer B

## 실행 및 격리

- Execution UUID: `53069a07-3f90-46d3-8438-450be9df060a`
- 시작 UTC: `2026-09-19T05:49:10.2757735Z`
- 종료 UTC: `2026-09-19T05:55:24.0628703Z`
- 역할: B / Huygens — 시험 oracle·변이 검출, SDK/CMake·CI 연결, 예산·실행근거·문서 정합성 및 delta 회귀.
- 저장소: `F:/dev/canview-wt/t007-ota-container`
- 확인한 base: `6cf1106bd1f7a2facf96e620727581e300822814`
- 확인한 candidate: `569cc83cfa73345c45c221e42ea51e196ab9c9ed`

시작·종료 모두 두 commit의 `git cat-file -e <hash>^{commit}` 성공과 `git rev-parse <hash>^{commit}`의 정확한 hash 일치를 확인했다.

저장소 내용은 고정 hash의 `git show`와 `git diff`로만 읽었다. moving worktree 소스를 사용하지 않았다. 파일 수정, 시험·컴파일, commit, 다른 agent 호출, 상대 finding 조회는 하지 않았다. 이전 native-post raw/report는 리뷰 대상에서 제외했다.

## 판정

**CONDITIONAL — 이 delta의 정적 리뷰에서 신규 P2 시험 oracle 결함 1건.**

현재 stage 구현의 실제 인증 우회나 잘못된 storage 호출은 확인하지 못했다. 다만 일부 인자 방어 시험은 해당 방어가 없어져도 통과할 수 있으므로 회귀 검증 보완이 필요하다.

전체 T-007 수용 판정의 기존 **BLOCK**, AC3·최종 target/예산 **OPEN**은 유지한다. 이번 판정은 Task 완료·merge·장치 설치 승인이 아니다.

## Findings

### P0: 없음
### P1: 없음

### P2 — B-STAGE-01: 잘못된 인자를 중첩한 open 시험이 방어 삭제를 검출하지 못함

위치:

- `tests/ota/stage_probe.c:308–309` — 잘못된 storage 인자와 함께 prefix·identity·runtime·floor를 NULL로 전달.
- `tests/ota/stage_probe.c:312–323` — overlap 시험에서도 검사 대상 외 필수 인자가 NULL.
- 관련 방어: `shared/ota/src/stage.c:30–40`
- 대체 거절 경로: `shared/ota/src/body.c:68`, `shared/ota/src/manifest.c:487–489`

실패 시나리오:

1. `stage.c:30`의 `stage_overlaps(stage, prefix, size)` 조건만 삭제한 회귀를 가정한다.
2. `stage_probe.c:315`의 `item == 0`은 stage와 겹치는 prefix를 전달하지만, `:316`에서 runtime도 NULL로 전달한다.
3. 중첩 방어가 없어도 `body_open → manifest_preflight`가 NULL runtime 때문에 `CANVIEW_INVALID_ARGUMENT`를 반환한다.
4. 시험의 기대 status와 마지막 storage 호출 수 0 조건이 그대로 성립한다. 즉 prefix 중첩 방어가 검증된 것처럼 통과한다.

`hash.context` 중첩 시험(`:321–323`)도 같은 NULL runtime에 가려진다. storage callback을 NULL로 만드는 시험 역시 다른 필수 인자가 잘못되어 해당 callback 사전검사의 누락을 구별하지 못한다.

이는 코드 경로로 확인한 **정적 oracle 결함**이다. 해당 mutant를 이번 리뷰에서 생성·실행하지 않았다. 현재 production 방어 조건은 존재한다.

영향:

- 잘못된 입력을 거절한다는 결과만 확인하고, 의도한 방어 조건이 거절 원인인지는 입증하지 못한다.
- 향후 overlap 또는 callback 사전검사 누락이 현재 시험을 통과할 수 있다.
- 현재의 세 call-order mutant(`check_stage_oracle.py:24–29`)는 다른 조건을 겨냥하므로 이 누락을 보완하지 않는다.
- 행·분기 coverage 수치만으로 이 oracle의 유효성을 주장할 수 없다.

권고:

- 유효한 prefix·identity·runtime·floor·hash·storage 기준 입력에서 **한 번에 하나의 인자만** 잘못되게 만든다.
- 중첩 입력은 충분한 크기·정렬·수명을 가진 backing으로 구성하고, 의도한 사전 거절 및 callback 미호출을 확인한다.
- 적어도 prefix-overlap 또는 hash-context-overlap 조건만 삭제한 실제 C mutant가 해당 CHECK에서 실패하는지 추가한다. 컴파일 실패·crash·timeout을 검출 성공으로 취급하지 않는다.

### P3: 없음

## 공격 범위와 확인 결과

- **호출 순서:** `stage.c:45–51`, `:68–71`, `:85–90`에서 각각 body open 성공 후 begin, feed 성공 후 write, body finish 성공 후 native callback을 호출한다. 기존 body 연결에서 manifest 인증·identity/runtime·floor 검사가 hash 시작과 storage begin에 앞선다.
- **실패·재진입·cleanup:** FAILED 이후 진행 차단, callback 중 busy 거절, partial begin의 close 대상 등록, close 실패 후 재시도, 최초 오류 보존 경로를 읽었다. 신규 runtime 결함은 확인하지 못했다.
- **수신 oracle:** 잘못된 offset, hole, 마지막 byte 변조, 절단, NULL/초과 chunk, feed overlap, write/native 실패 및 cleanup 재시도 시험을 확인했다. 정상 경로는 실제 수신 offset과 입력 byte, hash 완료 상태를 storage 모형에서 대조한다.
- **실제 C mutant 검사 구조:** `check_stage_oracle.py:33–47`은 변경 anchor 개수, 컴파일 성공, baseline exit 0, mutant exit 1 및 CHECK 출처를 검사한다. 컴파일 예외·timeout을 PASS로 전환하지 않는다. 이번에 직접 실행하지는 않았다.
- **암호 범위:** CNG 경로는 outer 서명·hash를 실제 provider로 연결하지만 storage/native callback은 모형이다. 모형 성공을 장치 native 검증으로 확장하지 않는 설명을 확인했다.
- **CMake/CI:** shared library와 IDF component에 stage source가 포함된다. 일반 stage 시험은 host CTest에, CNG와 mutant 시험은 Windows 조건에 등록된다. 기존 workflow가 해당 CTest를 호출한다는 정적 연결을 확인했다. 신규 candidate CI 결과는 확인하지 않았다.
- **SDK 경계:** fixture main의 네 stage API 호출은 NULL 인자 링크 확인이다. 실제 stage 수신·Flash provider·장치 native 검증 실행 증거가 아니다.
- **문서·예산:** stage 크기·자체 frame 수치와 전체 SDK stack/heap/timing 미측정을 구분한다. `NATIVE_MATCHED`를 설치 승인으로 표현하지 않는다. T-204/T-107 writer 및 T-205 policy 소유권을 유지하고 AC3를 열어 둔다.
- **합성 digest:** T103 fixture와 helper 기대값의 변경 일치를 확인했다. digest의 원 산출물 재생성·독립 재계산은 하지 않았다.

embedded-cstyle → embedded-architecture → embedded-documentation skill을 적용하여 C 계약, 계층·소유권, 구현과 문서의 증거 수준을 대조했다. 수정은 하지 않았다.

## 실제 읽은 파일과 명령

핵심 신규 파일은 모두 직접 line-level로 읽었다.

| 파일 | 읽은 범위 |
|---|---|
| `shared/ota/src/stage.c` | 전체 1–116 |
| `shared/ota/src/stage.h` | 전체 1–97 |
| `tests/ota/stage_probe.c` | 전체 1–360 |
| `tests/ota/test_stage.py` | 전체 1–59 |
| `tests/ota/check_stage_oracle.py` | 전체 1–53 |
| `shared/ota/src/body.c`, `body.h` | 전체 1–257 / 1–105 |
| `shared/ota/src/floor.c`, `floor.h` | 전체 1–126 / 1–79 |
| `shared/ota/src/envelope.c` | 1–211 |
| `shared/ota/src/manifest.c` | constraints 관련 함수 및 370–505의 identity/runtime/preflight |
| `shared/ota/CMakeLists.txt` | 전체 1–6 |
| `firmware/components/canview_ota_parser/CMakeLists.txt` | 전체 1–11 |
| `CMakeLists.txt` | delta 및 55–62, 150–160 등록 경로 |
| `tests/fixtures/idf-ota-image/main/main.c` | 전체 1–52 |
| `.github/workflows/foundation.yml` | host CTest·target fixture build·artifact 귀속 경로 |
| `shared/ota/README.md` | stage 추가분 289–319 및 diff |
| `docs/tasks/T-007-ota-container.md` | 현재 stage 기록, 179–219 소유권·AC 및 delta |
| `docs/resume.md` | 현재 상태·stage·제약 관련 범위 및 delta |
| `docs/journal.md` | 이번 추가 기록 |
| `tests/hil/fixtures/t103-capture-only.jsonl`, `tests/test_t103_capture_helpers.py` | 합성 digest 변경 부분 |
| `AGENTS.md`, `docs/README.md` | 전체 |
| `docs/architecture/ota.md` | §7–9, §12 |
| `docs/adr/009-ota-native-image-alignment.md` | 전체 1–46 |
| `docs/runbooks/agent-workflow.md` | immutable 독립 리뷰·심각도·산출물 관련 절 |

실제 명령 형태:

```text
[guid]::NewGuid()
[DateTime]::UtcNow.ToString('o')
git cat-file -e <base-or-candidate>^{commit}
git rev-parse <base-or-candidate>^{commit}
git diff --find-renames <base> <candidate>
git diff --name-status <base> <candidate>
git show <candidate>:<위 파일 경로>
git diff --check <base> <candidate> -- . ':!docs/reviews/**'
```

`git show` 출력은 PowerShell로 행 번호를 붙이거나 관련 범위를 선택하여 읽었다. 외부 skill 세 파일은 해당 `C:/Users/digit/.codex/skills/.../SKILL.md`를 직접 읽었다.

리뷰 보존 artifact를 제외한 `git diff --check`는 exit 0이었다. 이는 whitespace 확인이며 시험 실행 결과가 아니다.

## 실행 근거 귀속·한계

작성자가 제공한 Debug/Release 150/150, model/CNG 각 27 groups, C mutant 3개, sanitizer·coverage, SDK build·metadata 검사·DWARF/frame 수치는 **작성자 실행 근거**다. 이번 리뷰의 독립 실행으로 계산하지 않았다. 이전 `6cf1106` CI/artifact 감사는 새 stage candidate의 CI 증거가 아니다.

이번 리뷰에서 다음은 **NOT_RUN / 독립 미확인**이다.

- host 시험·mutant·sanitizer·coverage 재실행
- SDK compile/link 및 ELF/MAP/BIN·예산 수치 재산출
- 신규 candidate CI와 원본 실행 로그·artifact 감사
- 실제 장치 PSA/native 실행, Flash 쓰기·전원 차단, 전체 stack/heap/timing 측정
- 정상 OTA owner·영속 policy·실제 설치 통합

핵심 신규 source 미열람으로 인한 incomplete는 없다. 다만 정적 리뷰이며, 기존 SDK 내부 구현·전체 native packager·과거 리뷰는 이번 delta 범위 밖으로 재감사하지 않았다.

**physical/HIL: NOT_RUN. vehicle TX: NO-GO.**

최종적으로 **B-STAGE-01 보완이 필요한 CONDITIONAL**이다. 현재 stage의 호출 순서 계약에 대한 정적 검토 결과만이며, 전체 T-007 완료나 merge 승인을 의미하지 않는다.
