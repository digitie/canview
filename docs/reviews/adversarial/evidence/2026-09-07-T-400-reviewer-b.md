# Reviewer B 독립 Object-only Adversarial Review

- Execution ID: `RB-20260907T212127+0900-f35779a`
- 시작/종료: `2026-09-07T21:21:27+09:00` / `2026-09-07T21:26:24+09:00`
- 전문 범위: SDK config, board/generator/CMake 정합성, safety boundary, negative·mutation/resource-exhaustion 검증, CI·generated drift integrity
- Base: `c8a725d0071e84a1329c30e2cca4409b4a42cfba`
- Candidate: `f35779a78603dd8241ead9a8baa7307d629bf33a`
- Isolation: `origin/codex/t400-bridge-handoff`를 fetch한 뒤 immutable commit object만 `git show`/`git diff`로 읽음. checkout, worktree source 수정, stage, commit, push 없음. 다른 reviewer/agent report 또는 finding은 열지 않음.

## 실제 실행 명령

```powershell
git fetch --no-tags origin refs/heads/codex/t400-bridge-handoff:refs/remotes/origin/codex/t400-bridge-handoff
git cat-file -e 'f35779a78603dd8241ead9a8baa7307d629bf33a^{commit}'
git rev-parse 'f35779a78603dd8241ead9a8baa7307d629bf33a^{commit}'
git diff --find-renames c8a725d0071e84a1329c30e2cca4409b4a42cfba f35779a78603dd8241ead9a8baa7307d629bf33a
git diff --check --find-renames c8a725d0071e84a1329c30e2cca4409b4a42cfba f35779a78603dd8241ead9a8baa7307d629bf33a
git rev-parse 'c8a725d0071e84a1329c30e2cca4409b4a42cfba^{commit}'
git rev-parse 'f35779a78603dd8241ead9a8baa7307d629bf33a^{commit}'
```

`git diff --check`은 성공했습니다. candidate source/config/test는 `git show f35779a...:<path>` 및 candidate diff로만 읽었습니다.

실제 읽은 주요 candidate 파일: workflow/root·board/component/main CMake, `boards.json`, `generate_boards.py`, `check_sdkconfig.py`, `check_esp32_core_coverage.py`, 양 ESP sdkconfig.defaults, 모든 변경 BSP `board.c`/`runtime.c`/`board_pins.h`, ESP runtime·pool interface/implementation, `firmware/app/esp_core.c`, ESP core/foundation tests 및 generator/sdkconfig tests입니다.

## Findings

### P0

없음.

### P1-01 — wrong-BSP rejection 전에 실제 app lifecycle이 잘못된 BSP GPIO를 실행함

- 위치:
  - `firmware/app/esp_core.c:12-26`
  - `firmware/communicator/esp32/bsp/runtime.c:9-12`
  - `firmware/diagnostic-bridge/bsp/runtime.c:9-12`
  - `tests/esp_core/test_wrong_bsp.c:40-44`
- 실패 시나리오: Communicator runtime object와 Bridge `board.c` object를 교차 링크하면, `app_main()`은 line 22에서 Bridge BSP의 `enter_safe_state()`를 먼저 실행합니다. 그 뒤에야 line 26의 runtime 함수가 profile mismatch를 검사합니다. 현재 cross-link test는 `canview_esp_board_runtime()`만 직접 호출하므로 이 선행 GPIO lifecycle을 전혀 실행·검증하지 않습니다.
- 영향: mismatch가 거부되더라도 잘못된 board profile의 GPIO output/input 설정이 먼저 수행됩니다. Communicator 물리 보드에서 Bridge GPIO5/4를, 또는 Bridge 물리 보드에서 Communicator safe-pin 목록을 건드릴 수 있습니다. safe GPIO와 wrong-BSP rejection의 안전 목적을 충족하지 못합니다.
- 권고: expected profile 검증을 `board.enter_safe_state()`보다 앞선 preflight 단계로 분리하십시오. 교차-link integration test는 실제 `app_main()` lifecycle을 실행해 mismatch에서 `canview_gpio_output/input` 호출 수가 0이고 terminal safe-idle로만 진입함을 확인해야 합니다.

**Merge blocker: P1-01이 해소되기 전 merge 불가.**

### P2-01 — `CANVIEW_BOARD_PROFILE`이 실제 board profile 입력을 bind하지 않고 board ID만 hash함

- 위치:
  - `tools/generate_boards.py:28-34`
  - `tools/generate_boards.py:93-100`
  - `tools/generate_boards.py:245-250`
- 실패 시나리오: 동일한 `bridge-r1-n8r2` ID를 유지한 채 pinmap, memory, required net, safe GPIO mapping이 변경된 BSP object와 runtime object를 교차 링크하면, profile 값은 ID 기반으로 동일합니다. 따라서 runtime의 comparison이 통과합니다.
- 영향: generator drift check는 하나의 일관된 source tree에서는 유효하지만, 이 handoff가 추가한 cross-object mismatch 방어는 동일 board ID의 stale/incorrect BSP revision을 탐지하지 못합니다.
- 권고: board-specific canonical manifest entry와 해당 pin source를 profile digest 입력으로 사용하십시오. 32-bit tag를 유지한다면 generator에서 collision을 fail-closed로 유지하고, 동일 ID의 pin/profile mutation이 tag를 바꾸며 cross-link rejection되는 negative test를 추가하십시오.

### P2-02 — runtime open의 문서화된 ISR 거부가 구현되지 않음

- 위치:
  - `firmware/interface/canview_esp_runtime.h:52-53`
  - `firmware/platform/esp32s3/core_runtime.c:281-320`
- 실패 시나리오: ISR에서 `canview_esp_board_runtime()` 또는 `canview_esp_runtime_open()`을 호출하면, open은 `xPortInIsrContext()`를 검사하지 않고 owner/tick을 읽고 runtime state를 초기화합니다.
- 영향: header가 약속한 ISR rejection 계약과 실제 동작이 불일치합니다. 현재 callback tests는 runtime을 정상 task context에서 연 뒤 callback ISR만 검사하므로 이 경로를 검출하지 못합니다.
- 권고: 모든 task API와 runtime state 변경 이전에 ISR guard를 추가하고, `fake.in_isr = true` 상태에서 `canview_esp_runtime_open()` 및 BSP wrapper 모두 `CANVIEW_INVALID_ARGUMENT`을 반환하며 runtime/port가 보존되는 test를 추가하십시오.

### P3

없음.

## 확인된 비-finding

- Bridge main component는 `canview_esp_core`, `canview_esp32_platform`, `canview_esp32_runtime`만 요구하며 Wi-Fi, ESP-NOW, HTTP, NVS, CAN TX component를 새로 연결하지 않았습니다. Bridge read-only privilege widening은 발견하지 못했습니다.
- generated board drift, sdkconfig mutation, profile collision, target artifact/SDK config CI gates는 candidate에서 유지·확장되어 있습니다.
- Communicator ESP와 Bridge BSP의 partial GPIO failure 시 all-pin attempt 및 first-error preservation 구현은 요구 범위와 일치합니다.

## 검토하지 못한 범위

- Host CTest/Python tests, ESP-IDF build, STM32 build, CI 실행 결과는 object-only isolation 조건상 실행·검증하지 않았습니다.
- Flash, provisioning, physical board, rail/reset/brownout, HIL, watchdog timing, vehicle CAN/capture 및 vehicle TX는 모두 `NOT_RUN`입니다.
- Physical/HIL gate는 여전히 `NOT_RUN`이며 CAN TX 권한을 의미하지 않습니다.

## Verdict

**BLOCK**

P1-01은 wrong-BSP mismatch가 거부되기 전에 실제 GPIO를 실행하게 하므로 merge blocker입니다.
