**Verdict: BLOCK — two P2 reliability findings remain.** The candidate’s 41 harness tests pass, but additional probes reproduce stale PASS evidence after parser failure and report output exceeding the validator’s size limit.

| Review identity | Value |
|---|---|
| Execution ID | `CV-HOSTILE-20260908-T500-A-07` |
| UTC start | `2026-09-08 13:19:44` |
| UTC end | `2026-09-08 13:22:56` |
| Candidate | `01632f7fee247c446c94d0e129367385319a9e63` |
| Base | `50410ba23fcecfa1f28cea837d04a061c201d648` |
| Repository | `F:/dev/canview-wt/t500-hil` |
| Runtime | Windows, `C:/Python314/python.exe`, Python 3.14.3 |
| Scope | Typed events, bounded serialization, terminal report state, first-failure identity |

The candidate and base resolved successfully; HEAD matched the candidate, and the base was its ancestor. Runtime modules and tests were inspected through immutable Git blobs. All 24 harness/test inputs matched the candidate after normalizing checkout line endings. Repository files were not modified. The tracked tree remained clean; four pre-existing untracked files and the status digest remained unchanged. No other reviewer archive was read or used.

**Findings: P0: 0 · P1: 0 · P2: 2 · P3: 0**

**A-07-P2-01 — Deep JSON decoding failure leaves a previous PASS report valid**

Locations:

- `F:/dev/canview-wt/t500-hil/tests/hil/scenario.py:84`
- `F:/dev/canview-wt/t500-hil/tests/hil/scenario.py:93`
- `F:/dev/canview-wt/t500-hil/tests/hil/run.py:373`

The nesting limit is checked after `json.loads`. A sufficiently deep document raises `RecursionError` during decoding, which neither exception boundary catches. The runner exits without publishing a terminal report for that invocation.

Reproduction performed:

1. Run the complete trusted host inventory with seed 7, producing a valid PASS.
2. Create a custom scenario containing an event field with 20,000 nested arrays.
3. Run that scenario into the same output directory.
4. Validate the existing output as PASS.

Exact fixture construction:

```python
sf.write_text(
    '{"schema_version":1,"id":"deep-probe","title":"probe",'
    '"suites":["host"],"mode":"CAPTURE_ONLY",'
    '"actions":[{"type":"event","fields":{"nested":'
    + '[' * 20000 + '0' + ']' * 20000
    + '}}],"expect":{}}',
    encoding='utf-8',
)
```

Observed evidence:

```text
fixture_bytes=40157
runner_exit=1
RecursionError: Stack overflow (used 2912 kB) while decoding a JSON array
report_unchanged=True
post_failure_validator_exit=0
status=PASS scenarios=12
```

The report SHA-256 before and after the failed invocation was identical:

```text
a1da3eb47bfd42f710633ca9dcc60a8bd21c65e212eaddee98558c2767d2e18d
```

**Impact:** The process fails nonzero, but an artifact consumer can still validate the previous full-suite PASS after the failed rerun.

**Recommendation:** Convert decoder recursion failures into `ScenarioError` so the runner publishes BLOCKED evidence. Enforce nesting limits before or during decoding where practical. Add a regression that starts with a valid PASS and verifies its replacement after this failure.

**A-07-P2-02 — Runner publishes reports larger than its validator permits**

Locations:

- `F:/dev/canview-wt/t500-hil/tests/hil/run.py:176`
- `F:/dev/canview-wt/t500-hil/tests/hil/run.py:184`
- `F:/dev/canview-wt/t500-hil/tests/hil/validate_evidence.py:31`
- `F:/dev/canview-wt/t500-hil/tests/hil/validate_evidence.py:40`

The writer has no report-size limit. An accepted scenario can remain within input and event-log limits while producing enough per-event checks to exceed the validator’s 8 MiB report limit.

Reproduction used a custom scenario with `expect={}` and these actions:

```python
actions = [
    {"type": "event", "kind": "CAN_TX",
     "fields": {"arbitration_id": 291}}
] + [
    {"type": "reset", "targets": ["target"] * 128}
] * 96
```

Observed evidence:

```text
scenario_input_bytes=126154
actions=97
events=24579
event_bytes=4262445
report_bytes=15758683
runner_exit=1
report_status=FAIL
verification=structural-only
validator_exit=1
validator_reason=report is too large
```

**Impact:** An accepted, bounded scenario produces a FAIL artifact that the supplied validator cannot consume. Event-log bounds do not bound report serialization consistently.

**Recommendation:** Share report-size limits between writer and validator. Bound report construction and publish a compact BLOCKED report when the limit is exceeded. Preserve first-failure information without silently truncating evidence.

**Requested defect-class verification**

| Probe | A-07 result |
|---|---|
| COMMAND_REPLAY result as list and dict | Both produce validated FAIL; previous PASS replaced |
| FEEDBACK_RESULT result as list and dict | Both produce validated FAIL; previous PASS replaced |
| CAN_TX arbitration ID as list and dict | Both produce validated FAIL; previous PASS replaced |
| Lone surrogate in scenario title, value, and key | BLOCKED, exit 2; previous PASS replaced |
| Lone surrogate passed directly to EventLog | `EventLogError`; records and byte offset unchanged |
| Injected analyzer exception | BLOCKED with `analysis_contract_error` |
| Remove results from claimed full PASS | Rejected |
| Truncate results and matching inventory metadata | Rejected |
| Delete inventory or referenced event file | Rejected |
| Selected trusted `brownout`, explicit expected selection | Accepted |
| Selected report without expectation or with wrong selection | Rejected |
| Later CAN_TX falsely identified as first | Rejected |
| Impossible or nonoffending first-violation location | Rejected |
| Edited custom invariant, check, or violation message | Rejected |
| Promote custom evidence to `trusted-replay` | Rejected |
| Alter seed identity or radio trace | Rejected by deterministic replay |
| Relabel host evidence as physical PASS | Rejected |
| Symlink/junction output redirection | Refused; external targets untouched |
| Hardlinked report/event destinations | Replaced safely; original targets unchanged |

Determinism also held for **36 scenario/seed pairs**: all 12 scenarios at seeds `0`, `1`, and `2**64-1`.

**First-failure interpretation**

Within the repeated-TX case, the validator preserves the first offending event and rejects substitution of a later TX.

Across different invariant classes, selection follows analyzer check order, as documented in `F:/dev/canview-wt/t500-hil/tests/hil/README.md:48`. A probe with an unsafe safety decision at sequence 1 followed by CAN_TX at sequence 2 produced a validated first violation at sequence 2, offset 187. Therefore, this implementation does **not** guarantee the earliest failure chronologically across all invariant classes. This is a documented contract limitation, separate from the two findings above.

**Files and execution record**

Source inspected:

- `F:/dev/canview-wt/t500-hil/tests/hil/adapter.py`
- `F:/dev/canview-wt/t500-hil/tests/hil/analyze.py`
- `F:/dev/canview-wt/t500-hil/tests/hil/events.py`
- `F:/dev/canview-wt/t500-hil/tests/hil/scenario.py`
- `F:/dev/canview-wt/t500-hil/tests/hil/run.py`
- `F:/dev/canview-wt/t500-hil/tests/hil/validate_evidence.py`
- `F:/dev/canview-wt/t500-hil/tests/test_hil_runner.py`
- Harness README, budget manifest, example rig configuration, and forbidden-TX fixture under `F:/dev/canview-wt/t500-hil/tests/hil/`.

The 12 trusted scenario inputs were loaded and checked against candidate blobs: brownout, can-load, duplicate-command, espnow-loss, feedback-mismatch, guardian-timeouts, radio-pressure, reset-matrix, resource-exhaustion, security-injection, stale-gates, and uart-faults.

Identity and source commands included:

```powershell
git -C F:/dev/canview-wt/t500-hil rev-parse '01632f7fee247c446c94d0e129367385319a9e63^{commit}' '50410ba23fcecfa1f28cea837d04a061c201d648^{commit}' HEAD
git -C F:/dev/canview-wt/t500-hil show 01632f7fee247c446c94d0e129367385319a9e63:tests/hil/analyze.py
git -C F:/dev/canview-wt/t500-hil ls-tree -r --name-only 01632f7fee247c446c94d0e129367385319a9e63 tests/hil tests/test_hil_runner.py
git -C F:/dev/canview-wt/t500-hil diff --quiet 01632f7fee247c446c94d0e129367385319a9e63 --
git -C F:/dev/canview-wt/t500-hil merge-base --is-ancestor 50410ba23fcecfa1f28cea837d04a061c201d648 01632f7fee247c446c94d0e129367385319a9e63
```

The same `git show <candidate>:<path>` operation supplied the other inspected modules and tests. PowerShell here-strings piped to `python -B -` compiled those immutable blobs into memory, ran `unittest.defaultTestLoader.loadTestsFromModule`, and executed the adversarial probes. **41 tests ran: zero failures, errors, or skips.**

Standalone reproduction commands used `C:/Python314/python.exe -B`, with repository working directory:

```text
tests/hil/run.py --suite host --seed 7 --output <temporary-output>
tests/hil/run.py --suite host --scenario-dir <temporary-scenarios> --output <temporary-output>
tests/hil/validate_evidence.py <temporary-output> --expect-status PASS
tests/hil/validate_evidence.py <temporary-output> --expect-status FAIL
```

All generated fixtures and reports were outside the repository in temporary directories.

Unreviewed or unexecuted: full repository regression, target firmware builds, MCU runtime behavior, concurrent-writer races, abrupt power-loss durability, and physical fault injection.

**Physical/HIL: NOT_RUN. Vehicle CAN TX: NO-GO. Final verdict: BLOCK pending disposition of A-07-P2-01 and A-07-P2-02.**
