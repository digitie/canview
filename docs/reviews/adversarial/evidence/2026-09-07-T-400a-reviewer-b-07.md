# T400a B-05/B-06 P1 두 번째 post-fix 재검토 보고서

- execution ID: `T400A-REVIEW-B-20260907-07`
- 시작: `2026-09-07 19:59:41 +09:00` (Asia/Seoul)
- 종료: `2026-09-07 20:02:49 +09:00` (Asia/Seoul)
- base: `22222902e6ba9d06c3b0a8b2c9f4cb980c4e03f6`
- candidate: `a5ae85b13a5f11e2b7b805907c85e99123be718f`
- verdict: **CONDITIONAL**

## 전문 범위와 격리

B-05/B-06의 candidate-artifact provenance P1에 한정해 clean PR merge ref target CI run `34112160182`, target job 성공, artifact ID·digest, 18개 manifest/hash 직접 대조 기록, target warning scan, source candidate `5d6fac4` 연결의 충분성을 검토했다. SDK config, board profile, generator, CI/evidence integrity에 대한 정적 일관성도 대조했다.

candidate 내용은 Git object-only 방식으로만 읽었다. current worktree, untracked, index, branch, PR, 외부 서비스는 읽거나 변경하지 않았고 build/test/flash/CI도 실행하지 않았다. 따라서 CI run 및 artifact의 원격 상태를 직접 재조회한 판정이 아니라, immutable candidate 안에 보존된 provenance 기록과 workflow의 정적 대조 결과다.

## B-05/B-06 P1 disposition

**FIXED — candidate-artifact provenance 범위에서 해소.**

`docs/reviews/adversarial/evidence/2026-09-07-T-400a-target-postfix.md:115`는 clean PR merge ref `2e49280b06271ab1509fa1b34181f81b09c2be31`가 base `2222290`에 source candidate `5d6fac4`를 적용했다고 기록한다.

같은 파일의 다음 근거는 이전 P1의 “temporary/dirty artifact만 존재” 결함을 해소한다.

- `:117` — `target-firmware-images` artifact ID `10015285217` 및 ZIP SHA-256.
- `:118` — `target-firmware-logs` artifact ID `10015286065` 및 ZIP SHA-256.
- `:119` — 두 artifact를 내려받아 18개 manifest entry의 path, byte length, SHA-256을 재계산해 `18/18` 일치했다고 기록.
- `:123`~`:128` — STM32 Debug/Release, Communicator ESP32, Diagnostic Bridge, Controller, public-component fixture의 18개 BIN/ELF/MAP hash.
- `.github/workflows/foundation.yml` — target job은 각 target 명령의 `warning:`, `CMake Warning`, linker warning, `error:`를 실패로 처리하고, 18개 산출물의 byte length·SHA-256 manifest를 생성한다. Bridge 실제 `sdkconfig` 검사도 포함한다(`:218`).

따라서 source candidate H1과 clean target artifact, manifest, warning scan 사이의 provenance chain은 candidate evidence 안에서 구체적·상호 일관되게 보존됐다. B-05/B-06 P1을 계속 열어 둘 근거는 확인되지 않았다.

## Finding

### P0

없음.

### P1

새 P1 없음. B-05/B-06 P1은 위 근거로 **FIXED**다.

### P2

없음.

### P3

없음.

## 별도 release condition

`docs/reviews/adversarial/evidence/2026-09-07-T-400a-target-postfix.md:130` 및 `docs/journal.md:7`은 동일 run의 전체 `windows-c99` job이 Doxygen archive download의 반복 빈 응답으로 실패했다고 명시한다.

- 실패 시나리오: Doxygen archive download가 retry 후에도 빈 응답으로 끝나 Windows C99 gate가 실패한다.
- 영향: target-firmware job 성공과 18/18 artifact 대조가 있어도 workflow 전체는 `failure`이며, CI 전체 PASS 또는 merge-ready를 주장할 수 없다.
- 권고: Doxygen download 실패 원인을 재검증한 새 Windows C99 CI run을 성공시키고, 그 결과를 evidence에 보존한 뒤에만 full-CI release condition을 해제하라.

이는 target artifact provenance P1 자체가 아니라 남아 있는 **release condition**이다. 이 재검토는 Windows C99 failure를 숨기거나 CI 전체 PASS로 전환하지 않는다.

## 미검토 범위

- CI run `34112160182`의 원격 로그, artifact archive, metadata, head SHA의 외부 서비스 직접 확인
- 실제 generated `sdkconfig`, target BIN/ELF/MAP, compiler/linker output의 재계산
- CMake/component 상세 구현, Controller·STM32·ESP runtime 전체, protocol/OTA/web/provisioning 구현
- diff 중 지정 전문 범위 밖의 변경
- current worktree, untracked, index, branch, PR 상태

## Physical/HIL NOT_RUN

다음은 모두 `NOT_RUN`이며, 본 object-only 재검토·host fixture·CI artifact 기록으로 대체되지 않는다.

- 보드 flash 및 boot/serial/ST-LINK 관찰
- power rail, reset, brownout, external pull/gate
- 장시간 PSRAM, clock, watchdog 안정성
- vehicle CAN 연결, capture/evidence, 차량 송신 안전 판정
- provisioning, key/eFuse, production security 절차
- TX release, control lease, hardware TX gate

## 실제 명령

```text
Get-Date -Format 'yyyy-MM-dd HH:mm:ss K'

Get-Content -Raw C:\Users\digit\.codex\skills\embedded-architecture\SKILL.md
Get-Content -Raw C:\Users\digit\.codex\skills\embedded-cstyle\SKILL.md

git cat-file -e "a5ae85b13a5f11e2b7b805907c85e99123be718f^{commit}"
git rev-parse a5ae85b13a5f11e2b7b805907c85e99123be718f
git diff --find-renames 22222902e6ba9d06c3b0a8b2c9f4cb980c4e03f6 a5ae85b13a5f11e2b7b805907c85e99123be718f

Get-Date -Format 'yyyy-MM-dd HH:mm:ss K'
```

candidate 파일은 아래의 정확한 명령으로만 읽었다.

```text
git show a5ae85b13a5f11e2b7b805907c85e99123be718f:<path>
```

성공적으로 읽은 candidate 파일 전체 목록:

```text
docs/reviews/adversarial/evidence/2026-09-07-T-400a-target-postfix.md
docs/reviews/adversarial/evidence/2026-09-07-T-400a-reviewer-b-05.md
docs/reviews/adversarial/evidence/2026-09-07-T-400a-reviewer-b-06.md
docs/reviews/adversarial/2026-09-07-T-400a.md
docs/resume.md
docs/journal.md
docs/reviews/README.md
.github/workflows/foundation.yml
firmware/boards/boards.json
tools/check_sdkconfig.py
tools/generate_boards.py
tests/foundation/test_sdkconfig.py
tests/esp_core/test_bridge_integration.c
```

`git diff --find-renames`는 실행했으나 도구 전송 한계로 표시 출력이 잘렸다. 본 재검토는 지정 파일의 직접 object read와 provenance chain 대조에만 근거했다.

## 최종 판정

**CONDITIONAL.** B-05/B-06 candidate-artifact provenance P1은 clean merge ref, target job, artifact ID/digest, 18/18 manifest/hash audit, warning scan, source candidate 연결 기록으로 해소됐다. 다만 Windows C99 job의 Doxygen download failure 재실행 성공과 모든 physical/HIL gate는 남아 있으므로, CI 전체 PASS·merge-ready·TX release를 주장할 수 없다.
