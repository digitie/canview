## T400a hostile review raw report

- 실행 ID: `T400A-REVIEW-A-20260907-02`
- 한국 현지 시작: `2026-09-07 16:36:25 +09:00`
- 종료: `2026-09-07 16:41:27 +09:00`
- Candidate: `5821fcab613b2a544df4cde14281b898fac10388`
- Base: `22222902e6ba9d06c3b0a8b2c9f4cb980c4e03f6`
- 격리: checkout/worktree 없이 `git show`/`git diff` object-only. 수정·stage·commit·push 없음. 시작 시 기존 untracked `docs/session-continuation-prompt.md` 존재.
- 다른 reviewer finding/report는 읽거나 공유하지 않음.

### 실제 읽은 파일

`AGENTS.md`, `docs/README.md`, `docs/resume.md`, `docs/runbooks/agent-workflow.md`, `C:/Users/digit/.codex/skills/embedded-architecture/SKILL.md`, `embedded-cstyle/SKILL.md`, `embedded-rtos-design/SKILL.md`, `embedded-isr-design/SKILL.md`, `firmware/app/esp_core.c`, `firmware/module/esp_core/health.c`, `firmware/module/esp_core/pool.c`, `firmware/interface/canview_esp_core.h`, `firmware/interface/canview_esp_pool.h`, `firmware/interface/canview_esp_runtime.h`, `firmware/platform/esp32s3/core_runtime.c`, `firmware/interface/canview_board.h`, `firmware/communicator/esp32/bsp/{board.c,board_pins.h,runtime.c}`, `firmware/diagnostic-bridge/bsp/{board.c,board_pins.h,runtime.c}`, `firmware/controller/bsp/{board.c,board_pins.h}`.

Scoped diff로 `firmware/communicator/stm32/bsp/board_pins.h`, `firmware/boards/boards.json`의 contract/object를 확인했다.

### 실제 실행 명령

```powershell
Get-Date -Format 'yyyy-MM-dd HH:mm:ss K'
Get-Content -Raw 'AGENTS.md'
Get-Content -Raw 'docs/README.md'
Get-Content -Raw 'docs/resume.md'
Get-Content -Raw 'docs/runbooks/agent-workflow.md'
git -c safe.directory=F:/dev/canview status --short --branch
git -c safe.directory=F:/dev/canview cat-file -e '5821fcab613b2a544df4cde14281b898fac10388^{commit}'
git -c safe.directory=F:/dev/canview cat-file -e '22222902e6ba9d06c3b0a8b2c9f4cb980c4e03f6^{commit}'
git -c safe.directory=F:/dev/canview rev-parse '5821fcab613b2a544df4cde14281b898fac10388^{commit}'
git -c safe.directory=F:/dev/canview rev-parse '22222902e6ba9d06c3b0a8b2c9f4cb980c4e03f6^{commit}'
git -c safe.directory=F:/dev/canview show -s --format='%H%n%P%n%aI%n%cI%n%s' 5821fcab613b2a544df4cde14281b898fac10388
git -c safe.directory=F:/dev/canview show -s --format='%H%n%P%n%aI%n%cI%n%s' 22222902e6ba9d06c3b0a8b2c9f4cb980c4e03f6
git -c safe.directory=F:/dev/canview diff --stat --find-renames 22222902e6ba9d06c3b0a8b2c9f4cb980c4e03f6 5821fcab613b2a544df4cde14281b898fac10388 -- 'firmware/**'
git -c safe.directory=F:/dev/canview diff --name-status --find-renames 22222902e6ba9d06c3b0a8b2c9f4cb980c4e03f6 5821fcab613b2a544df4cde14281b898fac10388 -- 'firmware/**'
git -c safe.directory=F:/dev/canview diff --no-ext-diff --find-renames --unified=120 22222902e6ba9d06c3b0a8b2c9f4cb980c4e03f6 5821fcab613b2a544df4cde14281b898fac10388 -- [scoped paths]
git -c safe.directory=F:/dev/canview ls-tree -r --name-only 5821fcab613b2a544df4cde14281b898fac10388 | rg '(^|/)(runtime|board)[^/]*\\.(c|h)$|canview_board|pinmap|boards\\.json$'
git -c safe.directory=F:/dev/canview diff --name-status --find-renames 22222902e6ba9d06c3b0a8b2c9f4cb980c4e03f6 5821fcab613b2a544df4cde14281b898fac10388 -- 'firmware/**/board*.c' 'firmware/**/board*.h' 'firmware/**/runtime.c' 'firmware/**/canview_board*'
git -c safe.directory=F:/dev/canview show 5821fcab613b2a544df4cde14281b898fac10388:<each listed candidate path> | ForEach-Object { $script:n++; '{0,4}: {1}' -f $script:n, $_ }
git -c safe.directory=F:/dev/canview show 5821fcab613b2a544df4cde14281b898fac10388:firmware/module/esp_core/health.c | Select-Object -Skip 99 | ForEach-Object { $script:n++; '{0,4}: {1}' -f ($script:n + 99), $_ }
git -c safe.directory=F:/dev/canview show 5821fcab613b2a544df4cde14281b898fac10388:firmware/module/esp_core/pool.c | ForEach-Object { $script:n++; '{0,4}: {1}' -f $script:n, $_ }
Get-Date -Format 'yyyy-MM-dd HH:mm:ss K'
```

### Findings

**P0: 없음 확인**

**P1-01 — watchdog deadline 검사 fail-open**

- 위치: `firmware/platform/esp32s3/core_runtime.c:140-147`, `firmware/module/esp_core/health.c:151-161`
- 시나리오: `esp_task_wdt_reset()` 또는 내부 SDK lock이 `deadline` 이후 완료되어도 `fed_at`은 호출 전 timestamp로 기록되어 성공 처리된다.
- 영향: 250 ms health deadline을 초과한 service가 정상 진척으로 기록되어 terminal fault/reset이 지연된다.
- 권고: watchdog reset 완료 후 monotonic timestamp를 다시 측정하고 `fed_at <= deadline`을 검증한다. SDK 호출 상한을 별도 시험으로 입증한다.

**P1-02 — 비소유 global TWDT 재설정 및 실패 rollback 부재**

- 위치: `firmware/platform/esp32s3/core_runtime.c:47-70`
- 시나리오: `esp_task_wdt_status(NULL)`가 `ESP_ERR_NOT_FOUND`인 경우 기존 global TWDT를 `esp_task_wdt_reconfigure()`로 변경한다. 이후 `esp_task_wdt_add()` 실패 시에도 변경을 되돌리지 않는다.
- 영향: 다른 RTOS task/idle subscription의 timeout·panic 정책이 바뀌며, boot 실패 뒤에도 global watchdog 상태가 오염된다.
- 권고: 소유권 없는 global TWDT를 재설정하지 말고, 명시적 init owner/rollback 상태를 관리한다. 다중 task subscription 시험을 추가한다.

**P2-01 — partial safe GPIO 실패 후 복구 경로 없음**

- 위치: `firmware/communicator/esp32/bsp/board.c:23-30`, `firmware/app/esp_core.c:34-43`
- 시나리오: GPIO 출력 하나가 실패하면 이후 safe output을 시도하지 않고 반환한다. watchdog 등록 전 실패면 app은 영구 idle에 머문다.
- 영향: 일부 출력이 reset/default 상태로 남고, 안전 상태 재시도·명시적 reset이 없다. 외부 TX gate/pull에 전적으로 의존한다.
- 권고: 외부 TX gate를 먼저 강제하고 모든 safe output을 계속 처리·검증하며, 실패 시 명시적 safe halt/reset 정책을 둔다.

**P3: 없음 확인**

### 미검토 범위

CMake/build 통합, 삭제된 ESP 테스트의 대체 여부, 전체 base 파일 원문, ESP-IDF SDK API 의미와 실제 latency, generator 입력 및 pinmap 원문, 외부 TX gate/pull/reset/brownout 회로, 다른 RTOS task/ISR/UART/radio/CAN/STM32 runtime, target compile 및 runtime 계측은 읽거나 실행하지 않았다.

Physical/bench/HIL/vehicle: **NOT_RUN**

## Verdict: BLOCK
