# T-104 B-09 원문 보존

아래 fence 내부는 reviewer final answer 원문이다. 일시적인 detached worktree의
절대 링크도 원문 그대로 보존하며 현재 문서 navigation으로 취급하지 않는다.

````text
T104-RB-20260909-09 — Reviewer B 재검토 결과입니다. **RB-01~04는 모두 CLOSED**이며, 소프트웨어 검증을 완료했습니다. 신규 P3 두 건이 남습니다.

- 실행: 2026-09-09 17:19:48~17:27:34, Asia/Seoul
- 전문 범위: SDK·generator·CMake·보안 경계·증거 일치성
- 리뷰 루트: `F:/dev/canview-wt/2026-09-09-t104-final-b`
- 후보: `14ea3c9bcad22ac27fa8989291ea54656f6f4952`
- 기준: `b17bdfc0bb2a1bfa9d300c1e7662cac05c96df40`
- 이전 후보: `bdc67981a67d059518e9122f2c4c3eef3c5a4825`

다른 리뷰어 보고서는 읽지 않았습니다. 소스 수정·commit·push·네트워크 작업은 하지 않았으며, 종료 시 detached HEAD와 빈 Git status를 확인했습니다. 구현 checkout에서는 지정된 증거 문서와 Debug/Release 산출물·로그만 읽었습니다. 새 빌드는 작업 폴더의 `work/`에 생성했습니다. 초기 Python 테스트가 내부 임시 파일을 OS TEMP에 생성한 뒤, 재실행에서는 TMP/TEMP도 `work/`로 고정했습니다.

검토 범위는 비리뷰 변경 49개 파일과 이전 후보 이후 변경입니다. 주요 확인 파일은 UART owner/adapter와 인터페이스, composition root, build metadata/linker, root·target·module CMake, protocol schema/generated header/codec, SDK oracle, target·coverage gate, fake register 모델, 회귀 테스트 및 관련 문서입니다.

| 이전 지적 | 판정 | 확인 근거 |
|---|---|---|
| RB-01 SDK 금지 설정 oracle 누락 | CLOSED | 누락 3개와 NVS 항목 추가. 삭제 변이 검사를 포함한 13개 테스트 통과 |
| RB-02 RX 오류 복구 손실 누락 | CLOSED | 알려진 unread 7바이트 누적, 알 수 없는 손실 별도 계수 및 포화 회귀 통과 |
| RB-03 HELLO build ID 미기입 | CLOSED | GNU build ID→BSP→config→HELLO 연결, reset 후 유지 및 ELF/BIN 결합 검증 통과 |
| RB-04 coverage 함수 기준·설명 불일치 | CLOSED | platform 함수 기준 100%, 실제 36/36 실행, 최종 출력에 기준 명시 |

신규 지적:

1. **RB09-01 · P3 — DMAMUX 테스트 상수가 고정 SDK와 다릅니다.**  
   [테스트 헤더 5행](/F:/dev/canview-wt/2026-09-09-t104-final-b/tests/stm32/fake_stm32/stm32g4xx_ll_dmamux.h:5)의 RX/TX 값은 7/8이지만, 설치된 CubeG4 1.6.3 헤더는 26/27입니다. 테스트는 같은 fake 상수와 비교하므로 이 차이를 검출하지 못합니다. 실제 target은 vendor 상수를 사용하므로 현재 타깃 라우팅 오류는 확인되지 않았습니다. fake 값을 SDK와 맞추고 독립 상수 비교에 포함하십시오.

2. **RB09-02 · P3 — watchdog 문서의 현재 worker 설명이 오래되었습니다.**  
   [core-bench.md 38행](/F:/dev/canview-wt/2026-09-09-t104-final-b/firmware/communicator/stm32/docs/core-bench.md:38)과 [README 47행](/F:/dev/canview-wt/2026-09-09-t104-final-b/firmware/communicator/stm32/README.md:47)은 필수 worker가 health 하나라고 설명하지만, 현재 `main.c`는 UART와 health 두 개를 등록합니다. watchdog vote/WCET 검토자가 현재 구성을 잘못 이해할 수 있으므로 두 worker와 각각의 실행 budget을 반영하십시오.

독립 실행 결과:

| 검증 | 결과 |
|---|---|
| 새 Clang Debug host build·전체 CTest | **120/120 PASS** |
| Python unit | **117개 PASS** |
| SDK config / STM32 core gate | **13/13, 7/7 PASS** |
| UART·board generator `--check` | PASS |
| UART owner coverage | 함수 100%, line 73.484%, branch 58.428% |
| UART adapter coverage | 함수 100%, line 79.581%, branch 58.385% |
| 새 Arm Debug / Release build | 모두 PASS |
| Debug | Flash 51,716 B, RAM 79,816 B, 최대 개별 stack frame 928 B |
| Release | Flash 39,720 B, RAM 79,816 B, 최대 개별 stack frame 992 B |

실행 명령은 `cmake -S … -B work/rb09-*`, `cmake --build … -j 4`, `ctest --output-on-failure -j 4`, `check_stm32_coverage.py --build …`, 두 Python gate 테스트 및 generator `--check`입니다. 타깃 post-build의 compile contract·stack·CAPTURE_ONLY·CMSIS 상수·FDCAN layout 검사도 통과했습니다.

첫 CTest의 Python 실패는 Git 소유권 검사로 commit identity를 얻지 못한 실행 환경 문제였습니다. 실행별 `safe.directory` 적용 후 전체 120개가 통과했습니다.

허용된 산출물 8개와 로그 2개의 SHA-256이 증거 문서와 모두 일치합니다. 리뷰 소스의 canonical digest도 다음과 일치합니다.

`8ccfe8d227ba3454dab51864cc65634f4922c5351aabe4296564061f63e47b56`

ELF 심볼·GNU note·BIN의 build ID 결합도 Debug/Release 모두 검증했습니다. 신규 Release BIN은 제공된 Release BIN과 SHA-256까지 동일합니다.

신규 P0/P1/P2는 확인하지 않았습니다. 위 P3 두 건의 정리를 조건으로 합니다. 최신 CI는 직접 실행하거나 확인했다고 주장하지 않습니다. 물리 UART 4 Mbps·PRBS·CTS 타이밍·장시간 부하·reset/brownout·실측 WCET/stack·HIL은 **NOT_RUN**이며, CAN TX는 기존 **NO-GO** 경계를 유지합니다.

CONDITIONAL
````
