# T-002 ESP-NOW v1.3 적대적 리뷰 원본 — Reviewer B

- 최초 실행 ID: `T002-ADV-20260906-104757Z-9cff518`
- 후속 실행 ID: `T002-POSTFIX-20260906-110629Z-52cd191`
- 최초 실행 시각: 2026-09-06 10:47:57–11:06:29 UTC
- 후속 실행 시각: 2026-09-06 11:06:29–11:15:05 UTC
- 최초/후속 결과: 모두 `BLOCK`
- 격리: detached reviewer worktree, 수정 없음
- 전문 영역: protocol/ABI·build reproducibility·test completeness

## 전달한 review request

T-002의 schema/generator/generated header와 golden·malformed·compatibility fixture를 독립적으로 검토한다. schema metadata가 실제 Python oracle·generated ABI·CTest gate에 연결되어 있는지, malformed/unknown extension/duplicate/reproducibility/closed-world inventory와 C99/C11 layout gate가 우회되지 않는지 공격한다. 작업공간은 수정하지 않는다.

## 최초 관찰 — `9cff518`

reviewer는 generator/check, CMake/CTest, Clang C99/C11 syntax check를 실행하고 다음 finding을 기록했다.

### P1

- Windows checkout의 CRLF 때문에 schema digest/generated artifact가 플랫폼에 따라 달라질 수 있다.
- singleton TLV duplicate가 허용된다.

### P2

- role/state/direction/idempotency field validator가 없어 contract metadata가 실제 message field를 가리킨다는 보장이 없다.
- generated output inventory가 closed-world가 아니어서 stale/extra artifact를 잡지 못한다.
- C11 static layout assertion은 C11에서 확인되지만 C99 target에서는 비활성이다. 명시적인 C11 compile/test target이 필요하다.
- CRC/magic schema metadata가 implementation의 hard-coded oracle과 연결되지 않는다.
- semantic/test completeness gaps가 있다.

최초 verdict는 `BLOCK`이다.

## 후속 관찰 — `52cd191`

다음 gate가 통과한 것을 확인했다.

- LF fix 후 Windows checkout drift가 해소되었다.
- generator checks, 20/20 protocol test, 37/37 CTest, coverage core line/function 100%, `canview_wire.c` branch 269/270 = 99.6296%, public fixture C99 syntax가 통과했다.
- 다만 singleton TLV duplicate P1이 여전히 열려 있다.
- contract validator, stale/extra artifact inventory, C99 assertion/enum-mask binding, CRC/magic binding, semantic test completeness가 여전히 열려 있다.
- pairing artifact는 metadata-only이고 canonical fields/mutation values를 실제로 검증하지 않는다.
- full target ESP-IDF/STM32/ESP32 build는 이 reviewer 환경에서 `NOT_RUN`이다.

후속 verdict는 duplicate singleton TLV P1 및 P2가 남아 `BLOCK`이다.

## 원본 보존 주의

이 파일은 후속 `f013b30` 재리뷰 이전의 역사적 결과를 보존한다. 후속 코드 수정 및 reviewer 재확인 결과는 통합 report의 disposition/post-fix 절에만 기록한다.
