# T-007 stage post-fix2 독립 재확인 — Reviewer B

## 실행·기준선·격리

- Execution UUID: `5214bfc6-dbb6-46ca-9e3c-1c96e3492313`
- 시작 UTC: `2026-09-19T06:15:39.2375375Z`
- 종료 UTC: `2026-09-19T06:16:39.3321754Z`
- 역할: B / Huygens — 시험 oracle·변이 검출·증거 귀속·문서 정합성.
- 저장소: `F:/dev/canview-wt/t007-ota-container`
- Base: `82c5193b8a3ab628089252de0ca29dab5ace6367`
- Candidate: `77b84cf07cd868f0f0b858a00a0ae8c9b3eb9ef4`

시작·종료 모두 두 commit의 `git cat-file -e` 성공과 `git rev-parse <hash>^{commit}`의 정확한 hash 일치를 확인했다.

저장소 내용은 고정 object의 `git show`/`git diff`로만 읽었다. moving worktree 소스, 실행 로그, 상대 리뷰 raw/report는 열람하지 않았다. 파일 수정·시험·컴파일·commit·다른 agent 호출은 하지 않았다.

## 최종 판정

**PASS — 이번 post-fix2 delta의 정적 검토 범위.**

**B-STAGE-01 P2: FIXED.** 기존 hash-context 사례와 잔존 identity-overlap 사례를 포함한 원 finding의 수정이 확인됐다. 신규 P0/P1/P2/P3 finding은 없다.

이는 시험 실행 PASS, 신규 candidate CI 성공, 전체 T-007 완료 또는 merge 승인이 아니다. 전체 수용 감사와 기존 AC3·최종 target/예산 gate는 별도로 남는다.

## B-STAGE-01 수정 확인

### 기존 실패 시나리오와 영향

identity-overlap guard만 삭제하면 하위 manifest 초기화가 겹친 identity를 지우고, 결국 같은 `INVALID_ARGUMENT`를 반환했다. 기존 시험은 status와 storage 호출 수만 확인하여 이 잘못된 거절 경로를 통과시켰다. 영향은 사전 overlap 방어 누락을 검출하지 못하는 회귀 oracle 결함이었다.

### Candidate의 수정 근거

- `tests/ota/stage_probe.c:323–326`: 유효 입력의 open 성공·begin 1회·reset 성공·close 1회 양성 대조를 유지한다.
- `:342–345`: 중첩 입력이 manifest 영역 크기 안에 들어가는지 검사한다.
- `:350,355–360`: identity를 복사해 중첩 인자로 전달하고, 반환 status 확인 **직후 reset 전에** `stage.body.state == CANVIEW_OTA_BODY_EMPTY`를 검사한다.
- `:338,366,371`: storage 인자, hash context, 초과 범위 사례에도 같은 사전 거절 상태 검사를 적용했다.
- `tests/ota/check_stage_oracle.py:31`: identity-overlap 조건만 `false`로 바꾸는 다섯 번째 실제 C mutant를 추가했다.

거절 경로를 직접 대조했다.

1. 정상 guard는 `shared/ota/src/stage.c:30–35`에서 `stage_fail()`로 반환한다. `:15–19`는 stage 상태만 바꾸므로 body는 EMPTY다.
2. identity guard 삭제 시 `stage.c:45`에서 body open에 진입한다.
3. `manifest.c:486`, `:381–386`의 초기화·identity 검사가 기존과 같은 `INVALID_ARGUMENT`를 만든다.
4. 하지만 `body.c:83–85`의 실패 처리와 `:21–24`에 의해 body 상태는 FAILED가 된다.
5. 따라서 새 `stage_probe.c:360` CHECK가 reset으로 상태가 지워지기 전에 이 경로를 검출한다.

이 검출에는 잘못된 callback context 역참조나 crash가 필요하지 않다. hash-context 시험의 역참조 없는 `argument_hash_start()`(`:298–301`)도 유지됐다.

`check_stage_oracle.py:33–49`는 baseline exit 0, mutant exit 1, CHECK 출처, 단일 변경 anchor를 검사한다. 컴파일 실패·timeout·예외를 PASS로 바꾸지 않는다.

**권고 이행 판정:** 이전에 요청한 최소 상태 검사와 단일 조건 mutant가 반영됐다. 이 finding을 닫기 위한 추가 구현이나 framework는 필요하지 않다. 위 판단은 정적 경로 검토이며, 본인이 mutant를 실행했다는 뜻은 아니다.

## P0–P3 신규 finding

- P0: 없음.
- P1: 없음.
- P2: 없음. 기존 B-STAGE-01은 FIXED.
- P3: 없음.

추가 실패 시나리오·영향·수정 권고를 제시할 신규 결함은 확인하지 못했다.

## Delta 회귀·문서·증거 검토

- 새 CHECK는 모두 open 반환 뒤, reset 앞에 있다. reset이 EMPTY를 만들어 oracle을 만족시키는 순서 오류가 없다.
- 기존 single-invalid 양성 대조, hash-context 검출, call-order mutant 3개, 재진입·cleanup 시나리오는 유지된다.
- production C, firmware, root/shared CMake, workflow, `test_stage.py`가 base와 동일함을 diff로 확인했다.
- 기존 CTest의 `ota-stage-order`, Windows `ota-stage-order-crypto`, `ota-stage-oracle` 등록이 유지된다.
- `shared/ota/README.md:316–323`의 사전 거절 구분·mutant 5개 설명은 시험 source와 일치한다. SDK 연결은 compile/link·NULL 계약으로 제한하고 실제 writer/장치 실행으로 확대하지 않는다.
- `docs/journal.md:11–20`은 이번 작성자 실행과 `569cc83` CI, `82c5193` Linux 결과를 분리한다. 이전 source의 결과를 `77b84cf` 전체 성공으로 재사용하지 않는다.
- T103 fixture 5행과 helper의 기대 digest는 동일한 `f9a56ac269166efc8f20a3227786be47397437feb5e3403dfaa5dc2ec0f32870`로 바뀌었다. digest 재계산은 하지 않았다.
- 정적 예산 기록 부족의 이전 FIXED 판정은 유지한다. `shared/ota/README.md:325–353`의 정적 산술·자체 frame·SDK 내부 자원·장치 실측 구분은 변경되지 않았다.
- T-007의 다섯 AC는 미체크이며, `docs/tasks/T-007-ota-container.md:200–225`는 AC3·최종 target/예산·전체 수용을 계속 OPEN으로 둔다. T-204/T-107 완성품을 T-007 선행으로 순환 요구하지 않는다.

## 실제 읽은 파일·명령

| 파일 | 이번 직접 확인 범위 |
|---|---|
| `tests/ota/stage_probe.c` | 전체 1–406 |
| `tests/ota/check_stage_oracle.py` | 전체 1–55 |
| `shared/ota/src/stage.c` | 전체 1–116 |
| `shared/ota/src/stage.h` | 40–73 |
| `shared/ota/src/body.c`, `body.h` | 1–89 / 27–54 |
| `shared/ota/src/manifest.c` | 370–409, 477–505 |
| `shared/ota/README.md` | 311–353 및 전체 delta |
| `docs/resume.md` | 현재 상태·stage 기록·미완료 gate 및 전체 delta |
| `docs/tasks/T-007-ota-container.md` | 145–165, 185–226 및 전체 delta |
| `docs/journal.md` | 이번 추가분 1–24 |
| T103 fixture / helper | digest 변경 전체 |
| `CMakeLists.txt` | 55–62, 150–160 |
| `AGENTS.md`, `docs/README.md` | 전체 |
| `docs/runbooks/agent-workflow.md` | 107–120, 147–166 |

실제 명령 형태:

```text
[guid]::NewGuid().ToString()
[DateTime]::UtcNow.ToString('o')
git cat-file -e <base-or-candidate>^{commit}
git rev-parse <base-or-candidate>^{commit}
git diff --name-status --find-renames <base> <candidate>
git diff --find-renames <base> <candidate> -- . ':!docs/reviews/**'
git show <candidate>:<위 파일 경로>
git diff --exit-code <base> <candidate> -- shared/ota/src firmware CMakeLists.txt shared/ota/CMakeLists.txt .github/workflows/foundation.yml tests/ota/test_stage.py
git diff --check <base> <candidate> -- . ':!docs/reviews/**'
```

`git show` 출력에 PowerShell로 행 번호를 붙여 관련 범위를 읽었다. production/build/driver 동일성 검사와 리뷰 artifact 제외 whitespace 검사는 모두 exit 0이었다.

embedded-cstyle 및 embedded-documentation skill 원문을 읽고 상태·인자 경계와 설명의 일치를 확인했다. 수정 작업은 수행하지 않았다.

## 미검토·NOT_RUN·잔여 gate

핵심 delta 미열람에 따른 incomplete는 없다. 다만 이번 독립 검토에서 다음은 실행하거나 감사하지 않았다.

- host/CNG/mutant/ASan·UBSan 시험
- SDK compile/link, ELF/MAP/BIN 및 DWARF/stack 산출물 재검증
- 작성자 원 로그, CI 실행 상태·artifact 감사
- 합성 source digest 재계산
- 실제 device·Flash·HIL·전체 stack/heap/WCET 측정

Debug 150/150 27.52초, Release 150/150 23.42초, model/CNG 각 27 groups 및 sanitizer 결과는 **작성자 제공 근거**다. 본인의 실행 결과가 아니다.

남은 절차는 고정된 새 candidate의 CI·artifact 확인과 전체 T-007 수용 감사다. 이번 수정에 추가 소프트웨어 결함을 이유로 한 새 구현 요구는 없다.

**physical/device/Flash/HIL/total stack·heap·WCET: NOT_RUN. vehicle TX: NO-GO.**

**최종: B-STAGE-01 FIXED, 신규 finding 없음, post-fix2 정적 범위 PASS. 전체 T-007 DONE·merge 승인 아님.**
