# 독립 재검토 원문 보존

- 에이전트 ID: `01a07797-94d3-7000-8f0e-f561638b5734`
- 실행 입력: `313f180`을 기준으로 `027b22e`의 시험 delta를 Git object-only 방식으로 검토. 기존 자기 finding, 바이트 계수 soak, target source 동일성과 미실행 gate를 확인. 다른 reviewer 보고서는 열람하지 않음.
- 아래 제공자 원문은 수정하지 않고 보존한다.

# T-004 Post-fix Immutable Review

Execution ID: `f483499a-107a-44e0-a3d0-40d0145a8f6b`
Candidate: `027b22e6e22997e5405cbbdaeb95545037c55625`
Base: `313f180cf4d9990cd10c31a89db7e110116e4c22`
Review mode: object-only `git show/git diff`
Production C/target inputs: 변경 없음
Reviewer 보고서: 읽지 않음

## 확인 명령과 결과

- `git diff 313f180 027b22e --check` — PASS
- 전체 diff: `CMakeLists.txt`, `tests/protocol/test_uart.c`, `tests/protocol/uart_fault_stream.py`만 변경
- `shared/protocol`, `firmware`, `protocol/schema` 변경 없음 — PASS
- candidate Git object/hash 확인 — PASS
- candidate PR workflow 조회 — 아직 별도 run 없음
- 이번 리뷰에서는 detached worktree compile을 수행하지 않음 — NOT_RUN

## Soak harness 검토

`tests/protocol/test_uart.c:815-959`

확인된 내용:

- 양방향에 서로 다른 `canview_uart_codec_t` context 사용
- 요구 바이트 수:

  `seconds × 4,000,000 × 10 bits / 10 bits-per-byte`

  24시간 기준 방향별 `34,560,000,000` bytes
- 실제 production C decoder에 전달한 byte 수를 `uint64_t fed[]`로 계수
- 각 방향의 `fed >= required_bytes` 검증
- 변경/삭제/삽입/oversize fault 포함
- per-frame codec state, stream state, guard word 검증
- fault counter와 CRC/malformed/oversize counter 합산 검증
- CAN RX와 observer plan의 독립 codec context 사용
- `soak-smoke` CTest 등록 확인
- `soak-24h` 수동 실행 경로 확인

Soak smoke PASS는 사용자 제공 결과로 기록하며, 이번 immutable object-only 리뷰에서 직접 실행하지 않았습니다.

## 이전 finding disposition

- Cached archive SHA-256 P1: `313f180`에서 수정됨. `027b22e`에서 회귀 없음.
- Replay/session lifecycle: `6dc2218`에서 수정됨. 이번 diff에서 production code 변경 없음.
- Queue-full retry: enqueue 성공 후에만 replay sequence commit하는 구조가 유지됨.
- Windows CTest timeout: candidate에서 `300초` 유지.
- Current target evidence: candidate가 테스트 코드만 변경하므로 기존 production target evidence의 source binding은 유지됨. 신규 테스트가 target binary를 변경하지 않음.

## P0/P1 findings

없음.

## 남은 gate

### P1 gate — 24시간 실제 byte-accounted full run 미완료

코드와 harness 구조는 수용 기준을 충족하도록 보강됐지만, coordinator의 실제 full run 결과가 아직 없습니다.

필수 결과:

- 각 방향 `actual_bytes >= 34,560,000,000`
- fault 종류별 counter 일치
- guard/state corruption 없음
- 종료 시 stream 잔여 데이터 없음
- exit code 0

이 결과 전에는 T-004를 최종 완료로 표시할 수 없습니다.

## NOT_RUN

다음은 범위 밖이며 통과로 간주하지 않습니다.

- UART 4 Mbps 전기 파형
- RTS/CTS 전기·stall timing
- DMA/ISR ownership 및 timing
- 실제 local authentication/control-root 검증
- STM32 TX executor 및 CAN 송신
- 물리 보드/HIL/실차

## 최종 verdict

**CONDITIONAL**

코드·보안·생성 ABI·회귀 관점의 actionable P0/P1 결함은 없습니다.
단, 24시간 byte-accounted full soak 결과가 pending이므로 merge/completion gate는 **BLOCKED** 상태입니다.

## 서식 정정 기록

2026-09-07: `git diff --check`를 통과하도록 원문의 Markdown hard-break용 행 끝 공백만 제거했다. finding·심각도·verdict와 본문 내용은 변경하지 않았다.
