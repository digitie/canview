# T-007 C receiver SDK 연결 — Reviewer B 독립 리뷰 원문

## 실행·격리

- Execution UUID: `670747f4-bfbf-471d-a541-bdb37ce451d4`
- 시작 UTC: `2026-09-19T04:36:12.6002846Z`
- 종료 UTC: `2026-09-19T04:39:59.6224711Z`
- 전문: Reviewer B / Huygens — SDK/CMake 통합, 시험 oracle, 산출물·검증 근거.
- 저장소: `F:/dev/canview-wt/t007-ota-container`
- 실제 확인 base: `d87516a93439a5362a558b37ddb703633941c140`
- 실제 확인 candidate: `0acdc453cd8503631ee179c3bd8b49a1e30fce1f`

시작·종료 모두 `git cat-file -e "<hash>^{commit}"` 성공 및 `git rev-parse` 일치를 확인했다. 소스는 `git show candidate:path`와 commit 간 `git diff`로만 읽었다. moving worktree 소스·빌드 로그를 사용하지 않았다.

파일 수정·compile·시험 실행·commit·다른 agent 호출 없음. 보존 raw 및 다른 reviewer finding은 열지 않았다.

**Delta verdict: CONDITIONAL — 신규 P2 시험 oracle 보강 필요.**  
이전 전체 T-007 완료 감사의 BLOCK은 유지하며, 이번 결과는 전체 Task·target 실행·merge 승인이 아니다.

## 실제 읽은 파일·명령

직접 line-level로 읽은 핵심 범위:

- `firmware/components/canview_ota_parser/CMakeLists.txt:1–10`
- `tests/fixtures/idf-ota-image/main/receiver.c:1–102`
- 같은 경로 `receiver.h:1–15`, `main.c:1–46`, `CMakeLists.txt:1–8`
- `tests/fixtures/idf-ota-image/sdkconfig.defaults:1–7`, root `CMakeLists.txt:1–6`, `README.md:1–105`
- `tests/ota/idf_receiver_host.c:1–20`
- root `CMakeLists.txt:140–185` 및 delta
- `shared/ota/src/body.c:1–257`, `body.h:1–105`, `floor.c:1–126`
- `shared/ota/src/envelope.c:1–150`, `envelope.h:9–24`, `manifest.c:368–416,420–505`
- `firmware/platform/esp32s3/ota_crypto.c:1–186`, `ota_crypto.h:1–60`
- `tests/ota/cng_provider.c:1–133`, `cng_provider.h:1–28`
- 기존 OTA SDK component 및 `shared/ota/CMakeLists.txt`
- `AGENTS.md`, `docs/README.md`, `docs/resume.md:1–65`와 delta, T-007 상세의 신규 기록·구현 범위·수용 기준
- `shared/ota/README.md` 수정 절, `docs/journal.md:1–24`
- `.github/workflows/foundation.yml:21–44,294–312`
- T103 fixture·검사 코드의 digest delta
- 보존 `communicator.cvota`, `manifest-public.pem`의 Git blob

embedded-cstyle, embedded-architecture, embedded-documentation skill을 직접 읽고 수명·의존성·근거 분류에 적용했다. 자동 수정은 수행하지 않았다.

실제 명령 계열:

```text
[guid]::NewGuid().ToString()
[DateTime]::UtcNow.ToString('o')
git cat-file -e "<hash>^{commit}"
git rev-parse "<hash>^{commit>"
git diff --find-renames --name-status <base> <candidate>
git diff --find-renames <base> <candidate> -- <대상 경로>
git show <candidate>:<path>
git diff --exit-code <base> <candidate> -- <불변 경로>
git diff --check <base> <candidate> -- . ':!docs/reviews/**'
```

출력에 PowerShell 행 번호를 붙이고 필요한 구간을 선택했다. `rg -n`은 journal의 receiver 관련 기록 탐색에 사용했다. Binary SHA256 및 공개키 바이트 비교는 Git stdout을 메모리에서 처리했다.

## Findings

### P0: 없음
### P1: 없음

이번 delta에서 직접 확인한 신규 P0/P1 runtime 결함은 없다.

### B-RX-01 — P2 — 본문 변조 전에 발생한 오류도 본문 변조 시험 성공으로 집계된다

- 위치: `tests/fixtures/idf-ota-image/main/receiver.c:72–94`
- 관련 오류 경로: `tests/ota/cng_provider.c:99,109`, `shared/ota/src/body.c:167–170`

실패 시나리오:

1. 정상 case와 서명 변조 case가 기대대로 종료한다.
2. 본문 변조 case에서 마지막 byte를 바꾸기 전에 CNG `BCryptHashData`가 실패한다.
3. CNG provider는 이를 `CANVIEW_AUTH_FAILED`로 반환한다.
4. receiver는 본문 루프를 종료하고 마지막 byte 변조 코드 `:83–84`를 실행하지 않는다.
5. `:93–94`는 최종 오류가 `AUTH_FAILED`라는 이유만으로 이 case를 통과시킨다. cleanup과 identity case가 정상적으로 끝나면 전체 함수도 `true`를 반환한다.

manifest 검증이 먼저 `AUTH_FAILED`를 반환해 본문을 전혀 처리하지 않은 경우도 동일한 oracle 공백이 있다.

영향: 본문 변조 지점에 실제 도달하지 않아도 “4 cases matched”로 기록될 수 있다. 이는 시험 원인 판별의 결함이며, 현재 제품 입력의 인증 우회나 실제 SDK 장애가 관찰됐다는 뜻은 아니다.

권고:

- 본문 변조 case의 `body_open()`과 변이 전 모든 feed는 반드시 `CANVIEW_OK`여야 한다.
- 마지막 byte 변조 실행 여부를 확인하고, 그 feed에서 기대 오류가 발생했는지 비교한다.
- 변이 이전 `AUTH_FAILED`를 주입하는 제한 mutant가 반드시 시험을 실패시키는지 확인한다.
- 별도 framework 없이 현재 case별 조건 검사로 보강할 수 있다.

이번 리뷰에서는 fault injection이나 mutant를 실행하지 않았다. 위 경로는 직접 읽은 제어 흐름에 따른 정적 판정이다.

### P3: 신규 없음

기존 **B-T007-AUD-04 P3는 FIXED**다.

- `shared/ota/README.md:131–133`: 보존 signed golden을 별도로 연결하고 임의 bytes 시험과 구분했다.
- `shared/ota/README.md:248–250`: prefix·outer CLI·보존 golden은 구현됨으로 정정하고, 일반 native-aware 연결과 정상 owner 통합은 잔여 작업으로 구분했다.

## 공격 범위와 확인 결과

- **Chunk/offset:** prefix에서 요청량이 아닌 `consumed`만큼 전진한다. prefix 끝을 넘겨 제공한 입력의 나머지는 body 시작 offset부터 다시 읽으므로 손실되지 않는다. bundle 상한은 `uint32_t` 범위 안이다.
- **불변 입력:** 서명 변조는 prefix 복사본에만 적용한다. 마지막 body byte는 지역 `changed`로 전달하며 원본 golden을 수정하지 않는다. 현재 feed/provider는 호출 후 입력 pointer를 보존하지 않는다.
- **오류·cleanup:** 오류 뒤 body 진행을 중단한다. `body_reset()` 실패 시 `false`로 종료하고 static body/crypto를 보존하며 SDK close로 자원 수명을 뒤집지 않는다. 자동 재시도 완료를 주장하는 구현은 아니다.
- **단일 실행:** API는 single-task one-shot 계약이다. 호출자 두 곳도 한 번씩 호출한다. receiver 자체가 재진입 방어·mutex를 제공한다고 해석하지 않았다.
- **CMake:** 기존 portable 8개 소스를 재사용한다. fixture의 `native_metadata.c` 직접 편입을 제거해 새 parser component와의 중복 컴파일을 피했다. CNG 분기는 Windows host target에만 설정된다.
- **CI:** 새 CTest는 기존 Windows Debug/Release CTest 경로에 포함된다. Linux에서 이 CNG 시험을 실행한다고 주장하지 않는다. 기존 SDK build 뒤 metadata 검사 등록도 유지된다.
- **Host 파일 오류:** 인자·open·read error·길이·close 실패를 거절한다. `GOLDEN_BYTES + 1`을 읽어 초과 입력도 거절한다. 파일을 쓰지 않는다.
- **메모리·실행 근거:** 큰 context는 static이고 golden은 `EMBED_FILES`로 편입한다. stack 16384 예약과 `-fstack-usage` 추가는 확인했다. 실제 ELF 배치·총 stack·SDK heap·timing 충족을 소스만으로 확정하지 않았다. watchdog 변경은 delta에 없다.
- **권한:** native Flash adapter는 NULL negative 호출만 유지한다. 새 receiver는 outer manifest/hash 시험이며 Flash·PREPARED·boot selector·native 설치 승인을 호출하지 않는다.

직접 수행한 산출물 확인:

```text
communicator.cvota: 394310 bytes
SHA256: 68eb18e10bf35d351c1604500bf85f6e95aa41c6b49477ffbbdacd9477902655
receiver 공개 point 65B: 보존 manifest-public.pem의 P256 SPKI point와 일치
```

Portable 소스, PSA provider, 보존 golden 및 workflow는 base 대비 불변임을 diff로 확인했다. T103 두 파일의 새 합성 digest 문자열은 일치하지만 source identity 계산 자체는 재실행하지 않았다. Review 기록을 제외한 delta의 `git diff --check`는 성공했다.

## 검증 귀속·한계·최종 판정

작성자 제공 Windows Debug/Release `145/145`, CNG 4 case, SDK ELF/MAP/BIN 경고 0, metadata 변이·절단 및 DWARF/frame 수치는 **작성자 실행 근거**다. 본인은 해당 시험·build·로그·DWARF를 실행하거나 검사하지 않았다.

핵심 변경 소스는 직접 읽었다. 미검토 범위는 실제 SDK 내부 구현·생성 linker/ELF/MAP·CI artifact·동적 fault injection·전체 기존 시험의 재감사다. 새 private key 자료는 읽은 delta에 없으며, 저장소 전체 secret scan을 수행했다는 뜻은 아니다.

- Physical / PSA device / Flash / HIL: **NOT_RUN**
- Device 총 stack·heap·timing·watchdog 지연: **NOT_RUN**
- Vehicle TX: **NO-GO**
- 일반 native CLI·정상 owner 및 전체 acceptance: 기존 OPEN 유지

**최종: CONDITIONAL — B-RX-01 P2 oracle 보강 필요. README P3는 FIXED. 전체 T-007 완료 감사는 계속 BLOCK이며 merge 승인이 아니다.**
