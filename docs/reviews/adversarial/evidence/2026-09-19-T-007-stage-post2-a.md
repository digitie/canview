# T-007 stage post-fix2 독립 재확인 — Reviewer A

## 실행·격리

- Execution UUID: `f69f7800-a8a9-43d5-9d4a-a5966ca3f317`
- 시작 UTC: `2026-09-19T06:15:41.8978528Z`
- 종료 UTC: `2026-09-19T06:16:40.0968672Z`
- 역할: 원 Reviewer A / Maxwell — C 상태·메모리·수명·cleanup·권한 경계.
- 저장소: `F:/dev/canview-wt/t007-ota-container`
- 실제 확인 base: `82c5193b8a3ab628089252de0ca29dab5ace6367`
- 실제 확인 candidate: `77b84cf07cd868f0f0b858a00a0ae8c9b3eb9ef4`

시작·종료 모두 양쪽 commit의 `cat-file -e <hash>^{commit}` 성공 및 `rev-parse <hash>^{commit}` 일치를 확인했다. 저장소 소스는 고정 object의 `git show`·`git diff`로만 읽었다. Moving worktree 소스는 사용하지 않았다.

파일 수정·시험·compile·commit·다른 agent 호출은 하지 않았다. 보존 raw와 통합 review 본문, 상대 새 finding은 열람하지 않았다.

## 판정

**PASS — 이번 post-fix2 delta의 정적 검토에 한정한다.**

- **B-STAGE-01: FIXED — 알려진 hash-context 및 잔존 identity false PASS 경로의 수정 확인.**
- 신규 P0: 없음.
- 신규 P1: 없음.
- 신규 P2: 없음.
- 신규 P3: 없음.

이전 A 판정에서 hash-context 수정 확인을 B-STAGE-01 전체 해소로 확대한 것은 충분하지 않았다. 이번에는 동일 오류 반환 뒤의 body 상태까지 직접 추적했다. 이전 원문을 변경하거나 이번 결과를 소급 적용하지 않는다.

독립 실행 검증은 NOT_RUN이다. 전체 T-007 acceptance는 여전히 OPEN/BLOCK이며, DONE·merge 승인이 아니다.

## 원 finding 재확인

### B-STAGE-01 — identity overlap guard 삭제를 놓치는 시험

실패 시나리오:

1. `shared/ota/src/stage.c:30`의 identity overlap 조건만 삭제한다.
2. `tests/ota/stage_probe.c:350,355–358`에서 identity를 stage 내부에 놓고 open을 호출한다.
3. 하위 `manifest_preflight()`가 출력 manifest를 초기화하면서 중첩 identity도 지운다.
4. 잘못된 identity로 같은 `CANVIEW_INVALID_ARGUMENT`가 반환되므로 반환값만 검사하던 시험은 통과할 수 있다.

직접 확인한 연결:

- `shared/ota/src/stage.c:44–45`: body open 진입.
- `shared/ota/src/body.c:67–68`: manifest preflight 호출.
- `shared/ota/src/manifest.c:486`: 출력 manifest 초기화.
- `shared/ota/src/manifest.c:381–386`: 초기화된 identity의 role 검사에서 INVALID_ARGUMENT 반환.
- `shared/ota/src/body.c:83–85,21–30`: 오류를 body FAILED로 기록.
- `shared/ota/src/stage.c:54`: stage 실패 처리.

영향은 production guard가 삭제되어도 회귀시험이 이를 검출하지 못한다는 검증 공백이었다.

수정본은 `tests/ota/stage_probe.c:360`에서 **reset 전에 body EMPTY를 요구**한다. 따라서 위 mutant는 반환값 검사를 통과하더라도 body FAILED 때문에 CHECK 실패한다. 정상 stage의 조기 거절은 `stage.c:30–35`에서 body를 호출하지 않으므로 EMPTY가 유지된다. Reset은 그 다음 `stage_probe.c:361`에 있어 실패 흔적을 먼저 지우지 않는다.

동일한 상태 검사가 storage 인자군, hash context, SIZE_MAX 사례에도 추가됐다: `stage_probe.c:338,366,371`.

권고: 현재 반환값·reset 전 상태 검사와 identity mutant를 함께 유지한다. 이 수정으로 알려진 잔존 사례는 해소되며 추가 프레임워크는 필요하지 않다.

### 기존 hash-context 수정 유지

- 유효 입력의 begin 1회·close 1회 양성 대조: `stage_probe.c:323–326`.
- Hash context만 변경: `stage_probe.c:363–365`.
- 잘못된 context를 역참조하지 않는 start: `stage_probe.c:298–301`.
- 전체 인자 시험 뒤 저장 callback 횟수 확인: `stage_probe.c:373`.

Hash-context guard 삭제 시 open 성공을 거절 기대 CHECK로 검출하는 경로가 유지된다. 이번 EMPTY 검사가 기존 반환값 검사를 대체하지 않는다.

## 변이 실행기·메모리·회귀 확인

`tests/ota/check_stage_oracle.py:24–37`은 기존 네 변이에 identity-overlap을 추가하고 각 치환 anchor가 정확히 한 번 존재하도록 요구한다.

`check_stage_oracle.py:46–49`는 다음을 구분한다.

- Compile 실패: `check=True` 예외.
- Compile/실행 timeout: 예외.
- Baseline: exit 0 요구.
- Mutant: exit 1 및 `stage_probe.c:` CHECK 표식 요구.

따라서 compile 오류·timeout·일반 crash를 mutant 검출 성공으로 받는 경로는 확인되지 않았다. 다만 본 리뷰에서 실제 mutant를 실행하지는 않았다.

중첩 입력의 복사 크기는 `stage_probe.c:342–345`에서 manifest 저장공간 이하인지 검사한다. `stage.h:47`과 `body.h:43–53`의 배치상 복사 영역과 body 상태·hash 제어 필드는 분리된다. 새 EMPTY 읽기는 초기화된 유효 stage 객체를 대상으로 하며, reset 이후 상태를 검사하는 실수나 추가 포인터 수명 문제는 발견하지 못했다.

Production C, firmware, IDF fixture, 관련 CMake 및 workflow 경로의 diff는 비어 있었다. 기존 정상 흐름·재진입·partial begin·cleanup 재시도·최초 오류 보존 시험은 삭제되지 않았다.

## 문서·근거 귀속

- `shared/ota/README.md:316–323`: reset 전 EMPTY 검사와 다섯 mutant 설명이 코드와 일치한다. SDK 연결과 실제 장치 실행을 구분한다.
- `docs/journal.md:5–15`: 잔존 identity 사례와 재현·수정·최종 실행을 기록하고 새 candidate CI를 미완료로 둔다.
- `docs/journal.md:17–21`: `569cc83` CI와 `82c5193` Linux 결과의 귀속을 구분한다.
- `docs/resume.md:147–160`, `docs/tasks/T-007-ota-container.md:159–164,200–225`: 재확인·AC3·전체 수용 gate를 미완료로 유지한다.
- 합성 T103 fixture 5행과 `tests/test_t103_capture_helpers.py:26`은 같은 새 digest `f9a56ac269166efc8f20a3227786be47397437feb5e3403dfaa5dc2ec0f32870`으로 변경됐다. Digest 재계산은 하지 않았다.

Debug 150/150, Release 150/150, model/CNG 27그룹, ASan/UBSan 및 CI/artifact 수치는 **작성자 제공·문서 기록**이다. 본인의 실행 PASS로 집계하지 않는다.

정적 예산 절은 실측 미완료 구분을 유지하며 이번 delta에서 변경되지 않았다. 원산출물 재검증이나 전체 runtime budget 승인은 아니다.

## 실제 읽은 파일·명령

직접 열람:

- `AGENTS.md`, `docs/README.md`: 전체.
- `docs/resume.md:1–207`, `docs/tasks/T-007-ota-container.md:1–233`.
- `docs/journal.md:1–23`.
- `docs/runbooks/agent-workflow.md:105–139`.
- `tests/ota/stage_probe.c:1–406`, `tests/ota/check_stage_oracle.py:1–55`.
- `shared/ota/src/stage.c:1–116`, `stage.h:1–97`.
- `shared/ota/src/body.c:1–257`, `body.h:1–105`.
- `shared/ota/src/manifest.c:370–412,477–505` 및 관련 식별자 검색.
- `shared/ota/README.md:289–354`.
- T103 fixture와 helper의 변경 hunk.
- Embedded-cstyle·embedded-architecture·embedded-documentation `SKILL.md` 전체. 상태·수명·문서 근거 구분에 적용했으며 수정은 하지 않았다.

실제 명령 계열:

```text
[guid]::NewGuid().ToString()
[DateTime]::UtcNow.ToString('o')
git cat-file -e <base/candidate>^{commit}
git rev-parse <base/candidate>^{commit}
git diff --find-renames --name-status <base> <candidate>
git diff --find-renames <base> <candidate> -- <검토 대상 8개 경로>
git diff --name-only <base> <candidate> -- <production/SDK/build/workflow 경로>
git show <candidate>:<path>
git show <candidate>:<path> | rg -n <관련 식별자>
```

PowerShell로 표시용 행 번호를 부여했다. 출력이 잘린 task 본문은 별도로 다시 읽었다.

## 미검토·남은 gate

핵심 수정 source는 모두 읽었으며 미열람으로 인한 incomplete는 없다. 보존 review artifact, 실제 실행 로그·CI artifact, SDK 원산출물 및 변경 없는 전체 암호·parser 구현의 재감사는 하지 않았다.

남은 것은 고정 `77b84cf`의 CI·artifact 확인과 전체 T-007 수용 기준 감사다. 이전 candidate 성공을 새 candidate의 전체 검증으로 대체해서는 안 된다. 이번 수정 때문에 새로운 framework나 T204/T107 완성품을 선행 요구할 이유는 발견하지 못했다.

**Physical/device/Flash/HIL/total stack·heap·WCET: NOT_RUN. Vehicle TX: NO-GO. 전체 T-007 완료 및 merge 승인: 아님.**
