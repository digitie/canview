# Closure disposition 독립 감사 원문

- 에이전트 ID: `01a07797-94d3-7000-8f0e-f561638b5734`
- 요청: `4a0b5fa`의 통합 report에서 자기 기존 finding disposition을 감사. physical·local auth/TX의 범위 구분, 서식 정정과 남은 soak gate를 확인.
- Markdown hard-break용 행 끝 공백만 제거하고 아래 원문 내용·판정은 보존했다.

# T-004 closure disposition audit

검토 ID: `0c9fb9f7-e213-49d3-8ffd-ad65d7d1d2aa`
검토 시각: 2026-09-07 06:42 KST
현재 HEAD: `4a0b5fa0c40a3849e9e98e5aa4486aca28c5c144`
직전 commit: `3c6967aa83829cedbf7dcb4b54f67fd564e021e9`
검토 방식: immutable `git show` / `git diff`
다른 reviewer 원문: 읽지 않음

## 명령 결과

- `git rev-parse --verify HEAD^{commit}` — PASS
- closure commit stat 확인 — 문서/evidence만 변경
- production UART C 및 target input 변경 없음 — PASS
- `git diff --check` — PASS
- closure 상태 확인 — `VALIDATING`
- target evidence 확인 — 기준 source `6dc2218`, candidate production code와 source-equivalent

## Disposition 감사

### Physical UART/RTS/CTS requirement

Disposition: **오류 없음**

T-004 task 범위는 schema/codec/semantic state/host 검증이며 DMA/ISR·전기 RTS/CTS·물리/HIL은 후속 T-104/T-202/T-500 범위입니다.

따라서 해당 gate는:

- T-004에서 `FIXED`로 표시하면 안 됨
- `NOT_RUN` 및 후속 task 차단 조건으로 유지해야 함
- closure의 `REJECTED_WITH_EVIDENCE` 판정은 타당함

이는 physical safety 승인을 의미하지 않습니다.

### Auth / executor ownership

Disposition: **오류 없음**

closure와 public header는 다음을 올바르게 구분합니다.

- UART CRC/semantic codec은 인증이 아님
- local authorize callback은 exact validated request에 적용
- 실제 control-root, lease, safety snapshot, hardware TX gate, CAN executor는 T-105/T-106 소유
- 현재 T-004 codec 성공은 차량 admission 승인이 아님

후속 target executor가 이 계약을 반드시 구현해야 하며, 현재 문서가 이를 구현 완료로 과장하지 않았습니다.

### Payload ownership / session ownership

Disposition: **오류 없음**

- bounded enqueue callback의 synchronous deep-copy/rebind 계약
- single-owner, non-reentrant session transaction
- queue-full 시 replay sequence 미commit
- session reset 시 cache/plan/replay 무효화

가 명시되고, `3c6967a` regression evidence가 연결되어 있습니다.

### Cached archive SHA-256 P1

Disposition: **오류 없음**

`313f180`의 손상 cached archive 격리 후 verified download 재시도 수정이 보존되어 있습니다. `3c6967a` 이후 회귀도 없습니다.

### Full-rate soak P1

Disposition: **아직 OPEN — 올바른 상태**

정확한 기준은:

```text
4,000,000 baud / 10 bits per byte × 86,400 s
= 34,560,000,000 bytes per direction
```

`25.92GB/direction`은 요구량의 75%이며 완료 증거가 아닙니다. 따라서 현재 closure가 `VALIDATING`과 soak pending을 유지한 것은 올바릅니다.

주의할 점:

- `25.92GB/direction`만으로 T-004를 완료 처리하면 disposition 오류
- 최종 evidence에는 방향별 `actual_bytes >= 34,560,000,000`이 필요
- fault counter, guard, delimiter resync, 종료 stream 상태도 함께 기록해야 함

이전 제 보고서의 `×10 bits` 표현은 잘못된 설명이었고, 현재 closure 문서의 공식은 올바르게 정정되어 있습니다.

## 신규 actionable finding

- P0: 없음
- P1 코드 finding: 없음
- P1 gate: **full 34.56GB/direction soak 결과 pending**
- physical/HIL: 범위 밖 `NOT_RUN`

## 최종 판정

**BLOCK — full byte-accounted soak evidence pending**

Closure의 physical scope 및 auth/executor ownership disposition에는 오류가 없습니다. 다만 `25.92GB/direction`은 T-004의 4 Mbps 24시간 기준을 충족하지 않으므로, 최종 soak evidence가 제출되기 전까지 T-004 완료·merge 판정은 차단 상태로 유지해야 합니다.
