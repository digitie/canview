# T-104 B-11 원문 보존

Reviewer가 작성한 보고서 원문이다. 임시 경로는 원문 문맥이며 저장소 navigation이 아니다.

````text
# T104-RB-20260909-11 원문 보고서

- 시작: 2026-09-09T17:19:52.1003315+09:00
- 종료 검사: 2026-09-09T17:27:53.5069899+09:00
- 전문 범위: STM32 C99 runtime, DMA/ISR 소유권, session recovery, UART protocol/time synchronization, 시험·coverage·target artifact 연결.
- base: `b17bdfc0bb2a1bfa9d300c1e7662cac05c96df40`
- candidate: `14ea3c9bcad22ac27fa8989291ea54656f6f4952`
- 이전 기준: `bdc67981a67d059518e9122f2c4c3eef3c5a4825`
- 소스: `F:/dev/canview-wt/2026-09-09-t104-original-b-closure`

## 격리 및 범위

시작·종료에 candidate HEAD와 detached 상태를 확인했다. `git status --porcelain`은 모두 출력이 없었다. 종료 검사에서는 전역 ignore 읽기 권한 경고가 있었다. Git 소유권 검사에는 이 실행의 환경변수로만 safe.directory를 설정했으며 전역 설정은 변경하지 않았다. 소스·문서·Git 상태를 수정하지 않았다. build와 별도 artifact 검증 산출물은 본 작업의 `work/review11`에 썼다. 테스트 내부 임시 파일은 테스트가 OS 임시 디렉터리에 생성했다.

공통 manifest `git diff --name-only <base> <candidate> -- . ':!docs/reviews/**'`의 49개 변경 파일을 기준으로 전체 delta를 검토했다. manifest는 `scope-manifest.txt`에 보존했다. 새 UART owner/platform 구현과 헤더, 두 UART 시험 파일은 전체를 읽었고, 기존 파일은 변경 부분과 필요한 호출·계약 문맥을 읽었다. 모든 기존 파일의 변경 없는 전체 본문까지 재검토한 것은 아니다.

절차 예외: 원본 B 지적 ID를 찾는 마지막 `rg ... work -g '*.md'`가 보관된 이전 checkout까지 내려가 T-103/T-500 등 과거 타 리뷰 보고서 일부를 출력했다. 이 발췌를 이번 finding 또는 closure 근거로 사용하지 않았으며 현재 candidate에 대한 다른 reviewer 보고서를 찾아 읽지 않았다. 다만 타 리뷰 미열람 조건을 완전히 준수했다고 주장할 수 없다. 독립성 요건의 최종 수용 여부는 coordinator가 이 예외를 고려해야 한다.

## 새 finding

**RB11-01 — P3 — README의 scheduler 설명이 실제 UART 연결과 모순된다.**

- 위치: `firmware/communicator/stm32/README.md:47`, 같은 파일 `:65`.
- 시나리오: 현재 기본 app의 필수 health vote와 실행 worker를 README로 확인한다.
- 관찰: 47행은 필수 worker가 bench clock/time health 하나라고 쓰며 UART를 후속 연결 대상으로 기술한다. 65행은 기본 app의 CAN/UART worker가 시작되지 않는다고 쓴다. 실제 `app/main.c:83`의 worker 배열은 UART와 board health 두 항목을 모두 필수로 등록한다. README 3행과 46행도 이미 UART 연결을 명시한다.
- 영향: watchdog 진척 조건과 기본 실행 범위의 설명이 서로 달라 유지보수·시험 계획을 오도한다. 실행 코드 결함은 확인되지 않았다.
- 권고: 두 문단을 현재의 UART+board-health worker 구성으로 갱신하고 CAN 미연결 및 physical gate 설명을 정확히 유지한다.
- 상태: OPEN. 검토 요청에 따라 직접 수정하지 않았다.

이번 검토에서 새 P0/P1/P2는 확인하지 못했다.

## 원본 B-01~B-06 disposition

| ID | 원래 문제 | 판단 및 근거 |
|---|---|---|
| B-01 / P1 | CTS readiness 반전 | FIXED. `platform/stm32g474/uart_dma.c:252`의 GPIO high는 blocked로 해석된다. adapter 시험에서 high 구간에는 전송을 시작하지 않고 low 전환 후 시작함을 검증한다. |
| B-02 / P1 | TX DMA가 참조 중인 버퍼를 recovery가 무효화 | FIXED. `uart_dma.c:317`의 abort는 DMA channel과 USART DMAT를 끄고 barrier/readback을 수행한다. `:336` reset hook은 critical section에서 TX software event와 하드웨어 flag/pending을 정리한다. `module/uart_link.c:665` recovery와 `:1718` direct reset은 hook을 buffer 재사용 전에 호출한다. direct-reset 시험은 latched TC/TE와 RX event 보존, 후속 전송의 phantom completion 부재를 검증한다. 실제 MCU의 interrupt 지연·전기 동작은 별도 미실행이다. |
| B-03 / P2 | 독립된 clock epoch의 정상 time-sync 거부 | FIXED. `uart_link.c:1087` 부근에서 같은 clock domain의 timestamp 순서를 검사하고 cross-clock offset은 부호 있는 차이로 처리한다. 음수 offset 및 독립 epoch 회귀가 통과한다. |
| B-04 / P2 | COMMIT이 STM 측 저장 측정값에 결합되지 않음 | FIXED. `uart_link.c:1087`의 저장 t2/t3 일치 검사와 bounded 산술 검증을 확인했다. 조작된 측정값 및 만료 commit의 거부 시험이 포함되고 통과한다. |
| B-05 / P2 | 중단된 time-sync가 후속 교환을 무기한 막음 | FIXED. `uart_link.c:620`의 pending expiry가 tick 및 RX 처리 경로에서 실행되고 1000 ms 경계를 포함한다. 오래된 교환 제거와 후속 요청 수락 회귀가 통과한다. |
| B-06 / P2 | 두 UART 구현이 coverage qualification에서 제외 | FIXED. `tools/check_stm32_coverage.py`가 UART owner와 platform을 별도 그룹으로 계측·판정한다. 실제 실행에서 두 그룹 모두 function/line/branch 기준 100/70/50을 충족했다. 기존 그룹 기준을 UART에 맞춰 낮춘 것은 아니다. |

## 실제 실행 및 결과

도구는 제공된 LLVM 23.1.0, CMake 4.4.3, Ninja 1.13.2, Python 3.14 및 VS Developer PowerShell을 사용했다. 설치·반복 탐색은 하지 않았다.

```text
cmake -S F:/dev/canview-wt/2026-09-09-t104-original-b-closure -B work/review11/build -G Ninja -DCMAKE_C_COMPILER=clang -DCMAKE_BUILD_TYPE=Debug -DCANVIEW_COVERAGE=ON
cmake --build work/review11/build
ctest --test-dir work/review11/build -R 'stm32-uart|uart-|stm32-core|stm32-platform|sdkconfig|generated|python-unit' --output-on-failure --parallel 4
ctest --test-dir work/review11/build -R '^python-unit$' --output-on-failure
python -X utf8 -B <candidate>/tools/check_stm32_coverage.py --build work/review11/build
ctest --test-dir work/review11/build --output-on-failure --parallel 4
python -X utf8 -B work/review11/check_artifacts.py
git -C <candidate> rev-parse HEAD
git -C <candidate> symbolic-ref -q HEAD
git -C <candidate> status --porcelain
```

Configure/build가 성공했다. 첫 focused CTest는 33/34 성공했으며 python-unit은 Git 소유권 제한으로 source provenance가 누락되어 실패했다. subprocess에도 scoped safe.directory를 전달한 python 재시험은 성공했고, 이어 전체 CTest는 **120/120 성공(12.59초)** 했다. Python unit 내부 117개 시험이 포함된다. 실패 로그를 삭제하지 않았다.

Coverage는 7개 그룹 모두 통과했다. UART owner는 함수 73/73, 행 1394/1897(73.484449%), 분기 513/878(58.428246%)였다. UART platform은 함수 36/36, 행 608/764(79.581152%), 분기 188/322(58.385093%)였다. 이는 host/fake register 실행 증거이며 실제 peripheral 동작 증거로 확장하지 않는다.

로그: `configure.log`, `build.log`, `ctest.log`, `python-retest.log`, `coverage.log`, `ctest-full.log`, `artifacts.log`. `check_artifacts.py`는 candidate의 `check_build_id`를 사용하여 원본 ELF를 변경하지 않고 GNU build-id note를 scratch에 추출하고 ELF symbol/BIN 연결을 검사했다.

## 허용된 target 증거 대조

허용된 `F:/dev/canview-wt/t104-stm32-uart-control/docs/reviews/adversarial/evidence/2026-09-09-T-104-validation-14ea3c9.md`와 지정 Debug/Release build 디렉터리만 추가 읽기 대상으로 사용했다. Arm GNU 15.3.rel1 objcopy/nm으로 실제 ELF/BIN build ID 연결을 검증했다. 양쪽 BIN/ELF/HEX/MAP와 build log SHA-256이 문서 값과 모두 일치했다. 전체 값은 `artifacts.log`에 보존했다.

| 구성 | Build ID | BIN bytes | BIN SHA-256 |
|---|---|---:|---|
| Debug | `579f8e9de845abfc08abb195dfa5cf74` | 51716 | `865d82ebaf54d0b4dee61844fe3f7288a088ff6e6fd1dc64727b04a60f6d0f8b` |
| Release | `20cc3b17ecdec7df7dcc061ca8675d52` | 39720 | `5694eea4399f67954420aff56ad41db3e76792e8ededd9f11391ba0ca1a68469` |

증거 문서가 기록한 Windows Release/WSL/ASAN/TSan/strict target build 성공을 본인이 재실행한 결과로 계산하지 않았다. target를 새로 rebuild하지 않았으며 artifact 검사와 문서 대조만 수행했다. 최신 CI 판단은 coordinator 담당이다.

## 미검토·미실행과 최종 판단

manifest 내 변경 delta의 미검토 파일은 없다. 변경 없는 전체 저장소, 외부 SDK 전체, 실제 보드 flash/PRBS/RTS·CTS 전기 동작/DMA timing/물리 watchdog/HIL 및 full call-stack high-water 검증은 수행하지 않았다. 물리 HIL은 **NOT_RUN**이며 이 사실 자체를 software 검토 미완료로 취급하지 않는다.

원본 B-01~B-06은 모두 FIXED이다. 남은 새 finding은 문서 P3 한 건이다. 타 리뷰 발췌가 의도치 않게 출력된 절차 예외도 공개하며 독립성 수용 여부는 coordinator 판단이 필요하다.

**최종 verdict: CONDITIONAL**

## 실제 변경 검토 파일 목록

- CMakeLists.txt
- docs/api/Doxyfile
- docs/api/conf.py
- docs/api/index.rst
- docs/api/stm32_uart.rst
- docs/architecture/protocols/communicator-uart.md
- docs/resume.md
- docs/tasks.md
- docs/tasks/T-104-stm32-uart-control.md
- firmware/communicator/stm32/CMakeLists.txt
- firmware/communicator/stm32/README.md
- firmware/communicator/stm32/app/main.c
- firmware/communicator/stm32/bsp/build_metadata.c
- firmware/communicator/stm32/docs/core-bench.md
- firmware/communicator/stm32/docs/uart-dma.md
- firmware/communicator/stm32/interface/canview_stm_build.h
- firmware/communicator/stm32/interface/canview_stm_uart.h
- firmware/communicator/stm32/ld/STM32G474CEUx_FLASH.ld
- firmware/communicator/stm32/module/CMakeLists.txt
- firmware/communicator/stm32/module/uart_link.c
- firmware/communicator/stm32/platform/stm32g474/core_hw.c
- firmware/communicator/stm32/platform/stm32g474/core_hw.h
- firmware/communicator/stm32/platform/stm32g474/uart_dma.c
- firmware/communicator/stm32/platform/stm32g474/uart_dma.h
- firmware/communicator/stm32/tests/build_id_fixture.c
- firmware/communicator/stm32/tests/test_platform.c
- firmware/communicator/stm32/tests/test_registers.c
- protocol/canview_uart_protocol.h
- protocol/schema/uart-v1.0.yaml
- shared/protocol/include/canview_uart.h
- shared/protocol/src/canview_uart.c
- tests/foundation/CMakeLists.txt
- tests/foundation/test_sdkconfig.py
- tests/hil/fixtures/t103-capture-only.jsonl
- tests/protocol/test_uart.c
- tests/stm32/fake_stm32/board_pins.h
- tests/stm32/fake_stm32/core_hw.h
- tests/stm32/fake_stm32/fake_hardware.c
- tests/stm32/fake_stm32/fake_hardware.h
- tests/stm32/fake_stm32/stm32g474xx.h
- tests/stm32/fake_stm32/stm32g4xx_ll_dmamux.h
- tests/stm32/test_uart_dma_platform.c
- tests/stm32/test_uart_link.c
- tests/test_stm32_core_gate.py
- tests/test_t103_capture_helpers.py
- tools/build_docs.py
- tools/check_stm32_core.py
- tools/check_stm32_coverage.py
- tools/generate_uart_protocol.py


````
