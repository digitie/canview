# 독립 재검토 원문 보존

- 에이전트 ID: `01a07797-94d3-7000-8f0e-f561638b5734`
- 실행 입력: `027b22e`를 기준으로 `3c6967a`의 public ownership 계약, 실제 payload 덮어쓰기 회귀시험, UART companion 구분과 기존 finding을 독립 object-only 방식으로 검토.
- 아래 제공자 원문은 수정하지 않고 보존한다.

# T-004 post-fix immutable raw review

검토 시각: 2026-09-07 06:40 KST
Candidate: `3c6967aa83829cedbf7dcb4b54f67fd564e021e9`
Base: `027b22e6e22997e5405cbbdaeb95545037c55625`
검토 방식: object-only `git show` / `git diff`
생산 코드 compile: 수행하지 않음
다른 reviewer 보고서: 읽지 않음

## 실행 명령과 결과

- `git rev-parse --verify HEAD^{commit}` — PASS
- `git diff 027b22e 3c6967a --check` — PASS
- `git diff --name-status 027b22e 3c6967a` — PASS
- production source delta 확인 — header 주석/API 계약만 변경
- `shared/protocol/src/canview_uart.c` 변경 없음 — PASS
- `protocol/schema`, generated ABI, firmware target input 변경 없음 — PASS
- CMake soak-smoke 등록 확인 — PASS
- coordinator 제공 결과:
  - C soak-smoke — PASS
  - UART schema — 6/6 PASS
  - full byte soak — 실행 중

## 확인한 delta

변경 파일은 CMake, UART 테스트, schema 테스트, `canview_uart.h` 계약 주석 및 문서 evidence입니다.

header 계약은 다음을 명시합니다.

- queue callback은 `CANVIEW_OK` 반환 전에 header와 payload를 동기 deep-copy
- 저장 payload pointer는 owner queue slot으로 rebind
- codec backing storage pointer 보존 금지
- session/link/plan/cache/replay는 single-owner, non-reentrant worker가 관리
- full queue 시 replay sequence를 commit하지 않음

## Payload lifetime / queue retry

`tests/protocol/test_uart.c`의 신규 regression은 다음을 검증합니다.

- codec backing storage를 다음 frame으로 덮은 뒤 queue에 보관된 command payload가 변하지 않음
- 최소/최대 payload 길이 모두 검증
- full queue 반환 시 `replay.sequence`가 commit되지 않음
- queue owner slot의 payload pointer가 codec buffer와 다름
- owned copy 내용과 원본 payload가 일치

코드 구조상 production codec/session 동작 회귀는 확인되지 않았습니다.

## Soak byte-accounting

정정된 공식은 다음과 같습니다.

```text
4,000,000 baud / 10 bits per byte = 400,000 bytes/s
400,000 × 86,400 = 34,560,000,000 bytes per direction
```

이전 보고서의 “extra `×10 bits`” 표현은 잘못된 설명이며, 실제 요구 바이트 수는 방향별 `34,560,000,000` bytes입니다.

신규 C harness는:

- 양방향 distinct codec context
- `uint64_t` 실제 feed byte counter
- changed/deleted/inserted/oversize faults
- per-frame guard/counter
- `actual_bytes >= required_bytes`

를 포함합니다.

다만 full 24시간 soak 결과는 아직 pending입니다.

## 이전 finding disposition

- Cached archive SHA-256 P1: `313f180`에서 수정, candidate에 회귀 없음.
- Replay/session lifecycle P1: `6dc2218`에서 수정, candidate에 회귀 없음.
- Queue-full retry: sequence commit이 enqueue 성공 이후에 수행되는 구조 유지.
- Windows CTest timeout: `300초` 유지.
- Target evidence: production target input이 candidate에서 변경되지 않아 기존 `6dc2218` evidence와 source-equivalent.

## 신규 actionable findings

- P0: 없음
- P1: 없음
- P2/P3: 없음

## NOT_RUN

- full 24-hour byte soak 최종 완료 결과
- physical UART 4 Mbps waveform
- RTS/CTS electrical 및 stall timing
- DMA/ISR timing·ownership
- 실제 local authentication/control-root
- STM32 TX executor/CAN 송신
- board flash/boot, HIL, 실차

## 최종 verdict

- 코드/프로토콜/security regression: **PASS**
- 최종 acceptance gate: **BLOCKED — full byte-accounted soak pending**

full soak이 방향별 `34,560,000,000` bytes 이상과 fault/guard/counter 검증을 모두 통과하면 T-004 최종 PASS로 전환할 수 있습니다.

## 서식 정정 기록

2026-09-07: `git diff --check`를 통과하도록 원문의 Markdown hard-break용 행 끝 공백만 제거했다. finding·심각도·verdict와 본문 내용은 변경하지 않았다.
