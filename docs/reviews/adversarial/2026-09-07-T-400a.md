# 2026-09-07 T-400a Bridge 최소 core와 ESP 공용화 적대적 review

- 상태: `BLOCK`
- task: [T-400a](../../tasks/T-400a-bridge-core-bench.md)
- 원 candidate: `5821fcab613b2a544df4cde14281b898fac10388`
- base: `22222902e6ba9d06c3b0a8b2c9f4cb980c4e03f6`
- post-fix source candidate: `5d6fac46287886529f9b4eb27c487ed07b7c70d4`
- 이전 temporary alternate-object `85188da7321a656b6dc1a1c34cf6829fa6d41196`은 `.git` ACL 차단 때만 사용한 역사 기준선이다. 실제 PR branch의 source candidate는 `5d6fac4`이며, 이 report의 evidence 보강과 reviewer 재검토가 끝날 때까지 merge 성공을 주장하지 않는다.
- 범위: Communicator ESP32와 Diagnostic Bridge의 공용 C99 core/app, BSP/runtime, board/config generator, SDK config gate, host test/coverage, STM32 target build와 review/evidence integrity
- 범위 밖: 실제 board flash/HIL, ST-LINK/USB serial, power/reset/brownout, PSRAM/ECC/clock 장시간 계측, 차량 CAN/capture, security provisioning, TX release, T-400의 SoftAP/HTTP/WebSocket/인증 구현

## 1. 독립 reviewer 실행과 immutable 기준선

원 candidate에 대한 완전한 원 report는 삭제하거나 PASS로 바꾸지 않고 보존했다. post-fix reviewer는 같은 candidate manifest를 공유하지 않고 각각 독립적으로 요청했다. 결과가 반환되지 않은 실행은 완료 reviewer로 집계하지 않았다.

| 실행 | 전문 범위 | candidate/base | 격리·실행 결과 | 원문 |
|---|---|---|---|---|
| A-01 `T400A-REVIEW-A-20260907-01` | embedded runtime/RTOS/watchdog/reset/memory/GPIO/callback/isolation | `5821fca` / `2222290` | object-only, 16:14:11→16:36:18 KST, `BLOCK` | [A-01](evidence/2026-09-07-T-400a-reviewer-a.md) |
| A-02 `T400A-REVIEW-A-20260907-02` | 같은 전문의 제한 재검토 | `5821fca` / `2222290` | object-only, 16:36:25→16:41:27 KST, `BLOCK` | [A-02](evidence/2026-09-07-T-400a-reviewer-a-02.md) |
| B-01 `T400A-REVIEW-B-20260907-01` | SDK config/generator/CMake/security/CI/evidence | `5821fca` / `2222290` | archive/object-only, 16:22:57→16:49:15 KST, `BLOCK` | [B-01](evidence/2026-09-07-T-400a-reviewer-b-01.md) |
| B-02 `T400A-REVIEW-B-20260907-02` | 같은 전문의 제한 재검토 | `5821fca` / `2222290` | object-only, exact wall-clock 미기록, `CONDITIONAL` | [B-02](evidence/2026-09-07-T-400a-reviewer-b.md) |
| A-03 `T400A-REVIEW-A-20260907-03` | post-fix embedded runtime | `7e6adca` / `2222290` | primary tree 읽기 요청 후 120초·180초 wait와 interrupt에도 report 없음, shutdown | [incomplete](evidence/2026-09-07-T-400a-reviewer-a-03-incomplete.md) |
| B-03 `T400A-REVIEW-B-20260907-03` | post-fix config/build/evidence | `7e6adca` / `2222290` | primary tree 읽기 요청 후 120초·180초 wait와 interrupt에도 report 없음, shutdown | [incomplete](evidence/2026-09-07-T-400a-reviewer-b-03-incomplete.md) |
| A-04 `T400A-REVIEW-A-20260907-04` | 더 작은 post-fix embedded runtime 범위 | `85188da` / `2222290` | 120초·60초 wait와 interrupt에도 report 없음, shutdown | [incomplete](evidence/2026-09-07-T-400a-reviewer-a-04-incomplete.md) |
| B-04 `T400A-REVIEW-B-20260907-04` | 더 작은 post-fix config/build/evidence 범위 | `85188da` / `2222290` | 120초·60초 wait와 interrupt에도 report 없음, shutdown | [incomplete](evidence/2026-09-07-T-400a-reviewer-b-04-incomplete.md) |
| A-05 `T400A-REVIEW-A-20260907-05` | embedded runtime/RTOS/watchdog/reset/memory/GPIO/callback/isolation | `5d6fac4` / `2222290` | object-only, 19:37:46→19:42:18 KST, P0–P3 없음, `CONDITIONAL` | [A-05](evidence/2026-09-07-T-400a-reviewer-a-05.md) |
| B-05 `T400A-REVIEW-B-20260907-05` | SDK config/generator/CMake/security/CI/evidence | `5d6fac4` / `2222290` | object-only, 시작 `NOT_RECORDED`, 19:42:34 KST 종료, provenance P1로 `BLOCK` | [B-05](evidence/2026-09-07-T-400a-reviewer-b-05.md) |
| B-06 `T400A-REVIEW-B-20260907-06` | B-05 provenance P1 재검토 | `0146022` / `2222290` | object-only, 19:52:17→19:54:22 KST, clean CI artifact가 candidate에 없어 P1 `BLOCK` | [B-06](evidence/2026-09-07-T-400a-reviewer-b-06.md) |

A-03/B-03과 A-04/B-04는 각각 bounded retry 후 `close_agent`로 shutdown했다. 내부 시작·종료 시각과 실제 읽은 파일/명령을 service가 반환하지 않아 coordinator가 채우지 않았다. 따라서 네 실행 모두 `BLOCK (incomplete)`이며 source가 안전해 보인다는 이유로 verdict를 추정하지 않는다. A-05/B-05는 별도 새 execution ID로 source를 실제 읽은 완전 raw report이지만, B-05는 시작 시각이 `NOT_RECORDED`인 형식 제한과 candidate-artifact provenance P1을 함께 보존한다. 같은 reviewer의 B-06은 시작·종료 시각을 모두 기록했지만, `0146022` object에 CI artifact audit이 없음을 정확히 확인해 P1 `BLOCK`을 유지했다.

## 2. 원 finding과 disposition

중복 finding은 한 항목으로 묶되 원 report의 ID와 최고 심각도를 유지했다. `FIXED—재검토 대기`는 coordinator가 source와 회귀시험을 수정했다는 뜻이지 독립 post-fix PASS가 아니다.

| 원 finding | 최고 등급 | 근거·실패 형태 | 현재 disposition |
|---|---:|---|---|
| A-001 | P1 | `firmware/app/esp_core.c`에서 `runtime_open`이 실패하면 core 내부 `safe_gpio`에 도달하지 못했다. 출력이 외부 gate/pull/default 상태에 남을 수 있었다. | `FIXED—재검토 대기`: app이 `runtime_open` 전 `enter_safe_state`를 호출하도록 [esp_core.c:15-26](../../../firmware/app/esp_core.c) 수정했고 open/null callback 회귀시험을 추가했다. |
| A-002, A-02-P1-01 | P1 | `esp_task_wdt_reset()` 전 timestamp를 feed 성공 시각으로 기록해 reset/lock 지연이 deadline을 넘겨도 진척으로 승인할 수 있었다. | `FIXED—재검토 대기`: [core_runtime.c:128-142](../../../firmware/platform/esp32s3/core_runtime.c)에서 전·후 monotonic timestamp를 모두 검사하고 post-call 시각만 기록한다. reset 후 deadline 초과 fixture가 추가됐다. |
| A-003, A-02-P1-02 | P1 | current task가 소유하지 않은 global TWDT를 reconfigure하고 task 등록 실패 rollback이 없었다. | `FIXED—재검토 대기`: [core_runtime.c:40-56](../../../firmware/platform/esp32s3/core_runtime.c)는 global init/reconfigure를 하지 않고 IDF 초기화 상태에서 current task를 add한다. status/add 실패 matrix를 추가했다. |
| A-004 | P2 | sample 실패 뒤 이전 정상 sample이 stale 표시 없이 report됐다. | `FIXED—재검토 대기`: [health.c:14,95,163](../../../firmware/module/esp_core/health.c)에 `sample_valid` latch를 추가하고 report가 invalid sample을 0과 `sample-valid=0`으로 표시한다. |
| A-005 | P2 | 잘못 링크한 Communicator/Bridge BSP를 별도 board identity/profile binding 없이 memory/pin generic 검사만으로 통과시킬 수 있었다. | `DEFER`: owner=ESP BSP/runtime, 후속 `T-400`, gate `G1/board contract`, 목표 `2026-09-14`. compile-time board ID/profile hash binding과 wrong-BSP rejection을 별도 변경으로 남긴다. |
| A-006 | P2 | 공개 callback port는 task ownership만 확인하고 stage latch와 ISR-context 금지를 강제하지 않았다. | `DEFER`: owner=ESP core/runtime, 후속 `T-400`, gate `G1/RTOS-HIL`, 목표 `2026-09-14`. callback visibility/stage/ISR policy를 별도 설계·시험한다. |
| A-02-P2-01 | P2 | partial safe GPIO 실패 시 뒤의 safe output을 계속 검증하거나 복구하지 않아 일부 출력이 default 상태일 수 있었다. | `DEFER`: owner=BSP/hardware, 후속 `T-400`, gate `G1/power-reset`, 목표 `2026-09-14`. output ordering·all-pin failure policy와 외부 gate 실측을 함께 닫는다. |
| B-F-01, B-02-P2 | P1 | parser가 syntactically valid한 미검토 `CONFIG_*`와 selector를 허용해 security/resource/config omission이 통과할 수 있었다. | `FIXED—재검토 대기`: [check_sdkconfig.py:64-134](../../../tools/check_sdkconfig.py)에 IDF6.0.3 reviewed key allowlist, explicit disabled selector, mutually-exclusive flash selector를 추가했다. T400a는 NVS write/provisioning을 시작하지 않으므로 `CONFIG_NVS_ENCRYPTION`은 generated bench default에서 제거하고 forbidden으로 닫았다. production NVS encryption은 후속 provisioning gate이며 여기서 PASS로 주장하지 않는다. |
| B-F-02 | P2 | Bridge required net에 USB DM/DP가 없고 pin/name/electrical/package semantics mutation이 통과했다. | `FIXED—재검토 대기`: `boards.json` required net과 [generate_boards.py:22-24,104-133](../../../tools/generate_boards.py)에 exact USB contract와 post-map GPIO 19/20 check를 추가하고 mutation test를 추가했다. |
| B-F-03 | P2 | fake app test와 실제 SDK adapter test가 분리되어 Bridge app+BSP+runtime 조합의 실행 증거가 없었다. | `FIXED—재검토 대기`: root CMake의 `esp32-bridge-app-integration`과 [test_bridge_integration.c](../../../tests/esp_core/test_bridge_integration.c)는 generated Bridge memory/input/GPIO, safe→WDT→health→no-feed idle sequence를 실제 조합으로 실행한다. |
| B-05-P1, B-06-P1 | P1 | `5d6fac4` candidate 안의 target evidence가 원 candidate/temporary working-tree만 가리켜 candidate-귀속 target artifact provenance를 검증할 수 없었다. B-06은 `0146022` object에 clean CI artifact audit이 아직 없어 이를 그대로 `BLOCK`으로 유지했다. | `EVIDENCE 보강—동일 reviewer 재검토 대기`: `5d6fac4`의 local rebuild 명령/warning scan/hash와 PR merge ref의 clean target CI artifact 18/18 manifest/hash 대조를 [target evidence](evidence/2026-09-07-T-400a-target-postfix.md)에 기록했다. 이 보강을 포함한 새 immutable candidate의 B-07 raw verdict가 나올 때까지 P1을 닫지 않는다. |

P0은 확인되지 않았다. A-05는 embedded runtime P0–P3가 없다고 확인했고 B-05/B-06은 SDK/config source에 새 P0/P2/P3가 없다고 확인했지만, B-05-P1/B-06-P1은 unresolved다. P2 세 건은 명시적으로 후속 task로 연기했으므로 review/Task closure 조건을 아직 충족하지 않는다.

## 3. coordinator 변경

- `app_main()`이 runtime adapter를 열기 전에 필수 `enter_safe_state`/`idle` callback을 검증하고 safe state를 먼저 적용한다. safe/preflight failure, null callback, open failure에서 report/feed/re-init가 발생하지 않는다.
- ESP runtime의 watchdog은 global TWDT 설정을 가로채지 않고 current task subscription만 요청한다. feed는 reset 전후 시간을 확인하며 post-call timestamp를 core에 전달한다.
- fault 뒤 `sample_valid=false`를 유지해 stale memory/heap/GPIO 값을 정상 snapshot으로 보이지 않게 한다.
- Communicator와 Bridge를 실제 generated Bridge BSP/runtime까지 연결하는 host integration CTest를 추가했다. 이 fixture는 SDK API model이며 physical board/HIL이 아니다.
- ESP-IDF 6.0.3에서 산출된 Communicator/Bridge `sdkconfig` key union을 [allowlist](../../../tools/sdkconfig-allowlist/esp32s3-idf-6.0.3.keys)으로 고정하고, unknown active key·삭제된 required-disabled line·Flash selector·forbidden mutation을 독립 시험한다.
- Bridge USB DM/DP의 package pin 13/14, `USB_D-/USB_D+`, bidirectional type와 generated GPIO 19/20을 generator contract로 묶었다.

## 4. 실행된 검증

현재 working tree에서 실행한 결과다. build output은 repository tracked artifact가 아니며, 오래된 ESP build를 post-fix 성공으로 재사용하지 않았다.

| 영역 | 결과 |
|---|---|
| board/generated/config | `python -B tools/generate_boards.py --check` PASS, `tools/check_generated.py` 15 files PASS, `check_sdkconfig.py`가 보존된 IDF generated comm/bridge config를 각각 PASS |
| Python negative/unit | sdkconfig + generator 20 tests PASS, negative fixtures PASS, budget/plan/link validator PASS |
| host C | pinned Windows Clang/CMake/Ninja Debug와 Release에서 확장 CTest 각각 106/106 PASS. `esp32-bridge-app-integration` 포함, build warning 0 |
| long stream | `uart_fault_stream.py --seed 1 --duration-seconds 86400`: frames 1,720,000, corrupted 86,247, accepted 3,369,753, PASS |
| coverage | wire/app line/function 100%, branch 99.63%; ESP health/pool/runtime/app 각 function 100%, line 100%, branch gate ≥90%; STM32 host model 각 function 100%, line 100%, branch gate ≥90% |
| API docs | docs venv `pip check` PASS, Doxygen 1.18.0 + Sphinx 9.1.0 strict build와 public API 29개 brief/param/return PASS |
| target binary | STM32 Debug/Release와 post-fix Diagnostic Bridge·Communicator ESP32·Controller ESP32 target build가 모두 PASS했다. ESP32는 각각 app BIN/ELF/MAP 및 bootloader BIN을 새로 생성했고 실제 comm/bridge sdkconfig contract도 PASS했다. compiler/linker/CMake warning은 0이며 hash와 Kconfig notification은 [target evidence](evidence/2026-09-07-T-400a-target-postfix.md)에 기록했다. |

host fixture/coverage는 SDK/HIL 대체물이 아니다. post-fix ESP-IDF target compile은 별도 새 build directory에서 재실행해 닫았지만, 이는 physical/HIL·CI와 독립 reviewer gate를 닫지 않는다.

## 5. 미실행·외부 blocker

- `setup-windows.ps1 -VerifyOnly -ToolRoot C:/cv`: manifest checkout 확인 중 managed Git shell이 `basename`, `sed`, `git-sh-setup`을 찾지 못해 중단했다. 이 실패를 setup/target PASS로 표시하지 않는다.
- ASan/UBSan Linux 보조 gate: WSL `E_ACCESSDENIED`; current post-fix 실행 `NOT_RUN`. Windows host 성공으로 대체하지 않는다.
- board flash/HIL, ST-LINK/USB serial, power rail, reset/brownout, PSRAM/ECC/clock/watchdog 장시간 실측, vehicle CAN/capture/evidence, security provisioning과 vehicle TX release: 모두 `NOT_RUN`.
- 실제 branch commit/CI: `5d6fac4`는 PR #23 branch에 push됐고 CI run `34112160182`의 target-firmware job은 success 및 artifact manifest 18/18 대조를 완료했다. 전체 run은 Windows C99 job의 Doxygen archive 반복 빈 응답으로 failure다. B-05 P1 재검토와 전체 CI 재실행을 닫기 전에는 CI를 PASS로 기록하지 않는다.
- 사용자 소유 untracked `docs/session-continuation-prompt.md`는 stage·review·candidate에서 제외하고 보존했다.

## 6. 최종 판정과 다음 작업

T-400a는 `IN_PROGRESS`이며 이 report의 verdict는 `BLOCK`이다. A-05/B-05/B-06 원문은 보존됐지만 B-05-P1/B-06-P1, B-05 시작시각 형식 제한, CI 및 physical gate가 남아 있으므로 merge 또는 다음 T-400 구현으로 넘어가지 않는다.

다음 실행은 현재 writable `.git`과 GitHub/CI 연결이 있는 Windows session에서 수행한다.

1. evidence와 raw report를 명시 파일만 stage해 provenance 보강 commit으로 push한다. 사용자 prompt는 제외한다.
2. B-05/B-06 원 reviewer에게 clean CI artifact evidence가 포함된 immutable candidate의 object-only post-fix 재검토를 요청하고, 시작·종료 시각과 P1 disposition이 포함된 raw report를 받는다.
3. Windows C99 실패 log를 수정·재검증하고 clean CI의 ESP32/STM32 artifact와 warning/hash evidence를 확인한다.
4. unresolved finding·미실행 physical gate를 report/PR에 유지한 채 reviewer/CI closure 후에만 PR #23 ready/merge를 판단한다.

`T-400`의 board identity, callback stage/ISR, partial safe GPIO와 physical gate를 닫기 전에는 Diagnostic Bridge의 SoftAP/HTTP/인증 또는 차량 CAN/TX 권한을 추가하지 않는다.
