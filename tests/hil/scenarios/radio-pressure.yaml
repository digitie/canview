{
  "schema_version": 1,
  "id": "radio-pressure",
  "title": "SoftAP bridge observer and control radio budget pressure",
  "suites": ["host", "g2-readonly"],
  "mode": "CAPTURE_ONLY",
  "actions": [
    {"type": "radio_pressure", "softap_kbps": 80, "observer_kbps": 40, "control_kbps": 8, "rssi_dbm": -90}
  ],
  "expect": {
    "required_kinds": ["RADIO_BUDGET"],
    "minimum_counts": {"RADIO_BUDGET": 1},
    "required_fields": [{"kind": "RADIO_BUDGET", "fields": {"rssi_dbm": -90, "overflow_policy": "DROP_OBSERVER"}}],
    "ordered_events": [
      {"kind": "RADIO_BUDGET", "fields": {"softap_kbps": 80, "observer_kbps": 40, "control_kbps": 8, "rssi_dbm": -90, "overflow_policy": "DROP_OBSERVER"}},
      {"kind": "TX_GATE_STATE", "fields": {"vehicle_tx": false, "mode": "CAPTURE_ONLY"}},
      {"kind": "HARNESS_COMPLETE", "fields": {"scenario": "radio-pressure", "firmware_mode": "CAPTURE_ONLY"}}
    ]
  }
}
